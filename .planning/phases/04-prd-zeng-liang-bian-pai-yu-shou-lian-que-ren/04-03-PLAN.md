---
phase: 04-prd-zeng-liang-bian-pai-yu-shou-lian-que-ren
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - apps/web/src/lib/types.ts
  - apps/web/src/store/prd-store-helpers.ts
  - apps/web/src/store/workspace-store.ts
  - apps/web/src/components/workspace/prd-panel.tsx
  - apps/web/src/test/workspace-store.test.ts
  - apps/web/src/test/prd-panel.test.tsx
  - apps/web/src/test/workspace-session-shell.test.tsx
autonomous: true
requirements:
  - PRD-01
  - PRD-02
  - PRD-03
  - PRD-04
---

<objective>
消费 Phase 4 panel contract 到前端 store 和右侧 `PrdPanel`，完成章节化展示、本轮增量高亮、缺口提示和可确认初稿 CTA，同时保持 Phase 3 首稿卡片与 evidence drawer 的边界不变。

Purpose: 把后端已经统一的收敛语义真正交付给用户，而不是停留在 API 层。
Output: 新版 `PrdPanel` 与 store merge 逻辑。
</objective>

<context>
@.planning/phases/04-prd-zeng-liang-bian-pai-yu-shou-lian-que-ren/04-RESEARCH.md
@.planning/phases/04-prd-zeng-liang-bian-pai-yu-shou-lian-que-ren/04-02-PLAN.md
@apps/web/src/components/workspace/prd-panel.tsx
@apps/web/src/store/prd-store-helpers.ts
@apps/web/src/store/workspace-store.ts
@apps/web/src/lib/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 升级前端 PRD store，维护章节化 panel state、缺口与 changed section merge</name>
  <read_first>
    .planning/phases/04-prd-zeng-liang-bian-pai-yu-shou-lian-que-ren/04-02-PLAN.md
    apps/web/src/lib/types.ts
    apps/web/src/store/prd-store-helpers.ts
    apps/web/src/store/workspace-store.ts
    apps/web/src/test/workspace-store.test.ts
  </read_first>
  <files>apps/web/src/lib/types.ts, apps/web/src/store/prd-store-helpers.ts, apps/web/src/store/workspace-store.ts, apps/web/src/test/workspace-store.test.ts</files>
  <action>把前端 `prd` state 从旧 preview 模型升级到 Phase 4 panel projection。具体要求：1) types/store 至少支持 ordered sections、changed section keys、missing section keys、gap prompts、ready-for-confirmation/meta stage；2) hydrateSession、refreshSessionSnapshot、`prd.updated` event merge 必须共用同一套 normalize 逻辑；3) stale snapshot 保护必须继续生效，不能用旧 panel 覆盖新的 stream 结果；4) 维持 `firstDraft` state 与 `prd` state 分离，禁止因为 panel 升级把 `draft.updated` 重新写进右侧 panel；5) 测试需覆盖 hydrate、incremental merge、changed section 更新与 finalize 后状态切换。</action>
  <acceptance_criteria>
    - `rg -n "sectionsChanged|missingSections|gapPrompts|readyForConfirmation" apps/web/src/lib/types.ts apps/web/src/store/prd-store-helpers.ts apps/web/src/store/workspace-store.ts` 返回命中
    - `pnpm --filter web test -- src/test/workspace-store.test.ts -t "prd panel|gap|changed"` 通过
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter web test -- src/test/workspace-store.test.ts -t "prd panel|gap|changed"</automated>
  </verify>
  <done>前端 store 可以稳定表达 Phase 4 的 panel contract，SSE 与 snapshot merge 语义统一。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 重做右侧 PrdPanel 的章节化展示、增量高亮、缺口提示与确认 CTA</name>
  <read_first>
    .planning/phases/04-prd-zeng-liang-bian-pai-yu-shou-lian-que-ren/04-RESEARCH.md
    apps/web/src/components/workspace/prd-panel.tsx
    apps/web/src/test/prd-panel.test.tsx
    apps/web/src/test/workspace-session-shell.test.tsx
  </read_first>
  <files>apps/web/src/components/workspace/prd-panel.tsx, apps/web/src/test/prd-panel.test.tsx, apps/web/src/test/workspace-session-shell.test.tsx</files>
  <action>把右侧 panel 升级成 Phase 4 用户可见层。具体要求：1) `PrdPanel` 按固定章节顺序展示主章节和辅助章节，不再只是几张平面 section card；2) 明确标记本轮 changed sections，视觉上让用户看出“哪里更新了”，但不要对全部章节误高亮；3) 对缺失章节给出 gap prompt，文案要是“继续补什么”，不是复制 diagnostics ledger 明细；4) 当 `ready_for_confirmation` 达成时，展示“确认初稿 / 生成最终版 PRD”路径；5) finalized 后要收敛为稳定终稿视图；6) 维持 Phase 3 首稿卡片和 evidence drawer 仍在会话列，不把它们搬进右侧 panel。</action>
  <acceptance_criteria>
    - `rg -n "changed|gap|确认初稿|最终版" apps/web/src/components/workspace/prd-panel.tsx apps/web/src/test/prd-panel.test.tsx` 返回命中
    - `pnpm --filter web test -- src/test/prd-panel.test.tsx src/test/workspace-session-shell.test.tsx -t "section|incremental|confirm|gap"` 通过
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter web test -- src/test/prd-panel.test.tsx src/test/workspace-session-shell.test.tsx -t "section|incremental|confirm|gap"</automated>
  </verify>
  <done>用户在右侧 panel 能直接看到章节化 PRD、缺口和收敛状态，并清楚知道是否可以确认初稿。</done>
</task>

</tasks>

<verification>
前端验证必须同时覆盖：
- `PrdPanel` 只消费 Phase 4 panel contract
- changed section 高亮和 gap 提示都由 contract 驱动，不靠本地猜测
- Phase 3 first-draft / evidence UI 仍然留在会话列，没有被右侧 panel 吞并
</verification>

<success_criteria>
Phase 4 完成后，右侧 `PrdPanel` 会成为真正的“当前 PRD 收敛视图”：用户能看到章节、缺口、变化和确认入口，而不再只是简化 preview。
</success_criteria>

<output>
After completion, create `.planning/phases/04-prd-zeng-liang-bian-pai-yu-shou-lian-que-ren/04-03-SUMMARY.md`
</output>
