---
phase: 04-prd-zeng-liang-bian-pai-yu-shou-lian-que-ren
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/app/schemas/message.py
  - apps/api/app/services/prd_runtime.py
  - apps/api/app/agent/readiness.py
  - apps/api/app/services/message_state.py
  - apps/api/tests/test_readiness.py
  - apps/api/tests/test_messages_stream.py
  - apps/api/tests/test_sessions.py
autonomous: true
requirements:
  - PRD-01
  - PRD-02
  - PRD-03
  - PRD-04
---

<objective>
定义 Phase 4 的后端 panel contract：把 persisted `prd_draft`、diagnostics、critic/readiness 统一投影成章节化 PRD 视图，并明确本轮变化章节、缺口与“可确认初稿”状态。

Purpose: 先固定右侧 `PrdPanel` 的 authoritative projection，后续 SSE、snapshot、finalize 和前端 UI 才能共享同一份真相。
Output: `prd.updated` 的章节化 contract、readiness/gap projector、后端 contract tests。
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-prd-zeng-liang-bian-pai-yu-shou-lian-que-ren/04-RESEARCH.md
@apps/api/app/services/prd_runtime.py
@apps/api/app/agent/readiness.py
@apps/api/app/services/message_state.py
@apps/api/app/schemas/message.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 定义章节化 PRD panel projection contract，并从 persisted draft 统一投影</name>
  <read_first>
    .planning/phases/04-prd-zeng-liang-bian-pai-yu-shou-lian-que-ren/04-RESEARCH.md
    apps/api/app/services/prd_runtime.py
    apps/api/app/schemas/message.py
    apps/api/tests/test_messages_stream.py
    apps/api/tests/test_sessions.py
  </read_first>
  <files>apps/api/app/schemas/message.py, apps/api/app/services/prd_runtime.py, apps/api/app/services/message_state.py, apps/api/tests/test_messages_stream.py, apps/api/tests/test_sessions.py</files>
  <action>把 `prd.updated` 从旧的扁平 preview 升级成 Phase 4 panel contract。具体要求：1) 在 `PrdUpdatedEventData` 中明确 panel payload 至少包含 `sections`、`meta`、`sections_changed`、`missing_sections`、`gap_prompts`、`ready_for_confirmation`；2) `prd_runtime.py` 必须从 `state.prd_draft` 的 entry/completeness 结构统一投影章节内容，不允许前端自行把 first-draft 拼回 panel；3) 章节顺序至少覆盖 `target_user`、`problem`、`solution`、`mvp_scope`、`constraints`、`success_metrics`、`risks_to_validate`、`open_questions`；4) `risks_to_validate/open_questions` 只能使用摘要投影，禁止把 diagnostics ledger item 原文直接复制成正文 section；5) 对旧 `prd_snapshot.sections` 仍保留向后兼容回退路径，但仅作为 hydrate fallback，不得重新成为真源；6) 测试需要锁定 session snapshot 与 `prd.updated` 事件拿到的是同一份投影 shape。</action>
  <acceptance_criteria>
    - `rg -n "sections_changed|missing_sections|gap_prompts|ready_for_confirmation" apps/api/app/schemas/message.py apps/api/app/services/prd_runtime.py` 返回命中
    - `rg -n "risks_to_validate|open_questions|target_user|success_metrics" apps/api/tests/test_messages_stream.py apps/api/tests/test_sessions.py` 返回命中
    - `PYTHONPATH=apps/api uv run pytest apps/api/tests/test_messages_stream.py apps/api/tests/test_sessions.py -q -k "prd_updated or missing or confirmation"` 通过
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=apps/api uv run pytest apps/api/tests/test_messages_stream.py apps/api/tests/test_sessions.py -q -k "prd_updated or missing or confirmation"</automated>
  </verify>
  <done>右侧 panel 有了稳定的后端 projection contract，前端不再需要猜测章节结构或变化范围。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 升级 readiness / gap 判定到 entry-completeness aware，并产出可确认初稿状态</name>
  <read_first>
    .planning/phases/04-prd-zeng-liang-bian-pai-yu-shou-lian-que-ren/04-RESEARCH.md
    apps/api/app/agent/readiness.py
    apps/api/tests/test_readiness.py
    apps/api/tests/test_message_state.py
  </read_first>
  <files>apps/api/app/agent/readiness.py, apps/api/app/services/message_state.py, apps/api/tests/test_readiness.py, apps/api/tests/test_message_state.py</files>
  <action>把收敛判断从旧 `content/status` 升级到当前真实 persisted truth。具体要求：1) readiness 判定优先读取 `prd_draft.sections[*].entries` 与 `completeness`，空 entry 或 empty text 必须算缺口；2) `to_validate` 不等于 `missing`，但需要进入 panel 的“待验证 / 风险”摘要，并降低 `ready_for_confirmation`；3) `critic_result.major_gaps`、diagnostic summary 中的 open risk 应参与 gap 汇总；4) 输出状态至少区分 `drafting`、`needs_input`、`ready_for_confirmation`、`finalized`；5) 旧 `prd_snapshot` 回退路径依然可用，确保 legacy session 不崩；6) 测试必须覆盖“正文齐但仍有待验证项”“正文缺失”“legacy snapshot fallback”“终稿态”四类场景。</action>
  <acceptance_criteria>
    - `rg -n "ready_for_confirmation|needs_input|finalized|to_validate" apps/api/app/agent/readiness.py apps/api/tests/test_readiness.py` 返回命中
    - `PYTHONPATH=apps/api uv run pytest apps/api/tests/test_readiness.py apps/api/tests/test_message_state.py -q -k "readiness or prd"` 通过
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=apps/api uv run pytest apps/api/tests/test_readiness.py apps/api/tests/test_message_state.py -q -k "readiness or prd"</automated>
  </verify>
  <done>后端能够稳定判断“继续追问”与“给用户确认初稿”的切换时机，且不会把待验证误判成缺失。</done>
</task>

</tasks>

<verification>
后端验证必须同时覆盖：
- `prd.updated` 是章节化投影，而不是 raw first-draft
- `missing_sections/gap_prompts/ready_for_confirmation` 可从 snapshot 和 SSE 一致获得
- readiness 使用 entry/completeness 语义，而不是 Phase 0 的旧 section string 语义
</verification>

<success_criteria>
Phase 4 的 panel contract 完成后，系统已经能以统一的后端投影表达“当前 PRD 有哪些章节、哪些章节是本轮更新、还缺什么、是否可以给用户确认初稿”。
</success_criteria>

<output>
After completion, create `.planning/phases/04-prd-zeng-liang-bian-pai-yu-shou-lian-que-ren/04-01-SUMMARY.md`
</output>
