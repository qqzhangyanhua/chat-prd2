---
phase: 04-prd-zeng-liang-bian-pai-yu-shou-lian-que-ren
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - apps/api/app/services/messages.py
  - apps/api/app/services/sessions.py
  - apps/api/app/agent/finalize_flow.py
  - apps/api/app/services/exports.py
  - apps/api/tests/test_messages_service.py
  - apps/api/tests/test_messages_stream.py
  - apps/api/tests/test_sessions.py
  - apps/api/tests/test_finalize_session.py
autonomous: true
requirements:
  - PRD-02
  - PRD-03
  - PRD-04
---

<objective>
把 Phase 4 panel projection 接入消息流、snapshot、finalize 与导出兼容层，保证右侧 `PrdPanel` 在 streaming、刷新恢复和终稿化之后都看到同一份章节化结果。

Purpose: 让“章节化增量 PRD”从孤立的 projector 变成运行时真实链路，而不是只存在于局部 helper。
Output: SSE/snapshot/finalize/export 统一使用 Phase 4 projection contract。
</objective>

<context>
@.planning/phases/04-prd-zeng-liang-bian-pai-yu-shou-lian-que-ren/04-RESEARCH.md
@.planning/phases/04-prd-zeng-liang-bian-pai-yu-shou-lian-que-ren/04-01-PLAN.md
@apps/api/app/services/messages.py
@apps/api/app/services/sessions.py
@apps/api/app/agent/finalize_flow.py
@apps/api/app/services/exports.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 让 SSE 和 session snapshot 使用同一份 Phase 4 PRD projection，并锁定增量更新语义</name>
  <read_first>
    .planning/phases/04-prd-zeng-liang-bian-pai-yu-shou-lian-que-ren/04-01-PLAN.md
    apps/api/app/services/messages.py
    apps/api/app/services/sessions.py
    apps/api/tests/test_messages_service.py
    apps/api/tests/test_messages_stream.py
    apps/api/tests/test_sessions.py
  </read_first>
  <files>apps/api/app/services/messages.py, apps/api/app/services/sessions.py, apps/api/tests/test_messages_service.py, apps/api/tests/test_messages_stream.py, apps/api/tests/test_sessions.py</files>
  <action>把 Phase 4 的 panel projection 贯通到运行时链路。具体要求：1) `messages.py` 生成 `prd.updated` 时只能调用统一 projector，不允许零散手写 `meta/sections`；2) payload 里必须带 `sections_changed`，并保证只标记本轮受影响章节，而不是把全量章节都算 changed；3) `sessions.py` snapshot hydrate 必须返回与 SSE 同 shape 的 panel payload 或可稳定 derive 的等价结构，刷新后不能丢失 changed/missing/ready 信号；4) 需要保护 stale snapshot，不得用旧版本 panel 覆盖新一轮 stream；5) 若存在 legacy/未 enriched 会话，snapshot 仍需给出兼容投影；6) 测试需覆盖 stream、hydrate、refresh 恢复一致性。</action>
  <acceptance_criteria>
    - `rg -n "sections_changed|missing_sections|ready_for_confirmation" apps/api/app/services/messages.py apps/api/app/services/sessions.py` 返回命中
    - `PYTHONPATH=apps/api uv run pytest apps/api/tests/test_messages_service.py apps/api/tests/test_messages_stream.py apps/api/tests/test_sessions.py -q -k "prd_updated or snapshot or finalize"` 通过
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=apps/api uv run pytest apps/api/tests/test_messages_service.py apps/api/tests/test_messages_stream.py apps/api/tests/test_sessions.py -q -k "prd_updated or snapshot or finalize"</automated>
  </verify>
  <done>右侧 PRD 的 live update、刷新恢复和历史回放共用同一份投影真相，增量变化语义被锁定。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 让 finalize / export 与 Phase 4 收敛状态兼容，但不回退到整篇重写</name>
  <read_first>
    .planning/phases/04-prd-zeng-liang-bian-pai-yu-shou-lian-que-ren/04-RESEARCH.md
    apps/api/app/agent/finalize_flow.py
    apps/api/app/services/exports.py
    apps/api/tests/test_finalize_session.py
  </read_first>
  <files>apps/api/app/agent/finalize_flow.py, apps/api/app/services/exports.py, apps/api/tests/test_finalize_session.py</files>
  <action>把终稿化与导出衔接到新的章节化收敛语义。具体要求：1) finalize 入口必须消费 `ready_for_confirmation`/readiness projector 的结论，而不是旧的平面 section 字段判断；2) finalize 后 `prd.updated` / snapshot 需要明确进入 `finalized` 状态，并保留章节顺序与辅助章节；3) export 继续输出结构化 PRD，但不能因为 Phase 4 引入 panel projection 就回退成“把整篇文本一次性重新拼接”；4) 需要明确保留 `open_questions` / `risks_to_validate` 的结构化信息，至少在终稿后也能回放或导出摘要；5) 测试要覆盖 finalize 前后 panel 状态切换和 legacy enriched draft 的兼容。</action>
  <acceptance_criteria>
    - `rg -n "ready_for_confirmation|finalized|open_questions|risks_to_validate" apps/api/app/agent/finalize_flow.py apps/api/app/services/exports.py apps/api/tests/test_finalize_session.py` 返回命中
    - `PYTHONPATH=apps/api uv run pytest apps/api/tests/test_finalize_session.py -q -k "finalize or normalized"` 通过
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=apps/api uv run pytest apps/api/tests/test_finalize_session.py -q -k "finalize or normalized"</automated>
  </verify>
  <done>Phase 4 的章节化收敛结果可以顺畅进入终稿与导出链路，不会因为 finalize/export 把新语义压扁回旧模型。</done>
</task>

</tasks>

<verification>
集成验证必须同时覆盖：
- stream 与 snapshot 的 panel contract 一致
- `sections_changed` 在刷新后语义稳定
- finalize / export 使用新的 readiness 语义，而不是旧扁平 section 判断
</verification>

<success_criteria>
当用户推进对话、刷新页面、进入终稿或导出时，看到的都是同一套章节化 PRD 收敛结果，而不是不同路径各自拼装出来的近似物。
</success_criteria>

<output>
After completion, create `.planning/phases/04-prd-zeng-liang-bian-pai-yu-shou-lian-que-ren/04-02-SUMMARY.md`
</output>
