---
phase: 05-zhi-liang-fu-he-yu-jiao-fu-hui-fang
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - apps/api/app/services/exports.py
  - apps/api/app/api/routes/exports.py
  - apps/api/app/services/finalize_session.py
  - apps/api/tests/test_finalize_session.py
  - apps/api/tests/test_sessions.py
autonomous: true
requirements:
  - RVW-02
must_haves:
  truths:
    - "用户导出的 PRD 仍保留章节结构和待验证项，而不是退回成扁平整篇文本"
    - "导出复用后端现有 panel projection，并在需要时追加 review/handoff 摘要，不复制第二套投影逻辑"
    - "finalize/export 里程碑可被后续 replay 链路识别"
  artifacts:
    - path: "apps/api/app/services/exports.py"
      provides: "review-aware export builder"
    - path: "apps/api/app/services/finalize_session.py"
      provides: "可回放的 finalize/export 交付里程碑输入"
    - path: "apps/api/tests/test_finalize_session.py"
      provides: "导出/终稿回归"
  key_links:
    - from: "apps/api/app/services/exports.py"
      to: "apps/api/app/services/prd_runtime.py"
      via: "复用 panel projection sections"
      pattern: "build_prd_updated_event_data"
    - from: "apps/api/app/services/exports.py"
      to: "apps/api/app/services/prd_review.py"
      via: "拼接最小 review/handoff 摘要"
      pattern: "build_prd_review"
---

<objective>
扩展导出与交付链路：在不引入新持久化层的前提下复用 Phase 4 panel projection 和 Plan 05-01 review contract，产出结构化 PRD 文本及最小交付摘要。

Purpose: 让“确认后的可交付物”继续站在已有后端 projection 之上，避免 export 重新发明一套与 panel 脱节的内容拼装。
Output: review-aware export builder、finalize/export 回归测试、可复用的交付里程碑信息。
</objective>

<execution_context>
@/Users/zhangyanhua/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhangyanhua/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-zhi-liang-fu-he-yu-jiao-fu-hui-fang/05-RESEARCH.md
@.planning/phases/05-zhi-liang-fu-he-yu-jiao-fu-hui-fang/05-01-PLAN.md
@.planning/phases/04-prd-zeng-liang-bian-pai-yu-shou-lian-que-ren/04-02-SUMMARY.md
@apps/api/app/services/exports.py
@apps/api/app/api/routes/exports.py
@apps/api/app/services/finalize_session.py

<interfaces>
Current export entrypoint:

```python
@router.post("")
def export_session(...) -> dict[str, str]:
    return export_service.export_markdown(db, session_id, current_user.id)
```

Current builder boundary:

```python
def build_export_sections(state: dict, snapshot: dict) -> tuple[dict, bool]:
    panel_sections = build_prd_updated_event_data(state, {}, {}).get("sections", {})
```

Preserve this projection reuse. Extend the export payload/markdown shape around it instead of rebuilding from raw strings only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展 export builder，复用 panel projection 并保留待验证项与最小 review 摘要</name>
  <read_first>
    .planning/phases/05-zhi-liang-fu-he-yu-jiao-fu-hui-fang/05-01-PLAN.md
    apps/api/app/services/exports.py
    apps/api/app/services/prd_runtime.py
    apps/api/tests/test_finalize_session.py
    apps/api/tests/test_sessions.py
  </read_first>
  <files>apps/api/app/services/exports.py, apps/api/app/api/routes/exports.py, apps/api/tests/test_finalize_session.py, apps/api/tests/test_sessions.py</files>
  <action>按本 phase 约束扩展导出，不新建第二套 projection。具体要求：1) `build_export_sections()` 继续复用 `build_prd_updated_event_data(...)[\"sections\"]` 作为正文章节来源，这是本次运行的明确边界；2) 导出必须保留 `target_user/problem/solution/mvp_scope/constraints/success_metrics/risks_to_validate/open_questions` 等章节，并把待验证项明确写入最终 markdown，而不是在 finalize/export 时丢失；3) 可选追加一个轻量 review appendix 或 handoff summary，但必须与正文分区，不能把 review 文本混进 panel projection 内容；4) route 层若需要返回额外元数据，可扩展 response shape，但必须保持现有 `file_name` 与 `content` 向后兼容；5) 测试要覆盖 finalized 与非 finalized 两种导出，以及 review summary 不喧宾夺主、正文仍是章节化 PRD。</action>
  <acceptance_criteria>
    - `rg -n "build_prd_updated_event_data|risks_to_validate|open_questions|appendix|review" apps/api/app/services/exports.py apps/api/tests/test_finalize_session.py apps/api/tests/test_sessions.py` 返回命中
    - `rg -n "file_name|content" apps/api/app/api/routes/exports.py apps/api/app/services/exports.py` 仍然返回命中，证明兼容旧导出入口
    - `PYTHONPATH=apps/api uv run pytest apps/api/tests/test_finalize_session.py apps/api/tests/test_sessions.py -q -k "export or appendix or handoff"` 通过
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=apps/api uv run pytest apps/api/tests/test_finalize_session.py apps/api/tests/test_sessions.py -q -k "export or appendix or handoff"</automated>
  </verify>
  <done>导出链路继续站在共享 projection 之上，生成的 PRD 文本保留章节结构与待验证项，并能附带最小交付摘要。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 在 finalize/export 链路补齐可回放的交付里程碑，而不新增持久化表</name>
  <read_first>
    .planning/phases/05-zhi-liang-fu-he-yu-jiao-fu-hui-fang/05-RESEARCH.md
    apps/api/app/services/finalize_session.py
    apps/api/app/services/sessions.py
    apps/api/tests/test_finalize_session.py
  </read_first>
  <files>apps/api/app/services/finalize_session.py, apps/api/tests/test_finalize_session.py</files>
  <action>为后续 replay 做最小交付里程碑准备，但坚持“不新建持久化层”。具体要求：1) finalize 流继续复用现有 state/prd snapshot version 写入，不新增 `timeline_events`、`review_events` 等表；2) 在 finalize transition 或 export result 中保留足够的 milestone 信息供 replay 聚合识别，例如 finalized version、confirmation source、exported file name / exported at 的可推断字段或返回值；3) 所有新增信息都必须从现有 records 或服务输出中推导，不得引入额外 repository；4) 回归测试覆盖 finalize 后导出与里程碑字段存在性，确保 Plan 05-03 可以在不加存储的前提下聚合 replay timeline。</action>
  <acceptance_criteria>
    - `rg -n "confirmation_source|finalize_preference|prd_snapshot_version|file_name" apps/api/app/services/finalize_session.py apps/api/tests/test_finalize_session.py` 返回命中
    - `rg -n "timeline_events|review_events" apps/api/app/services/finalize_session.py apps/api/app/services/exports.py` 无命中
    - `PYTHONPATH=apps/api uv run pytest apps/api/tests/test_finalize_session.py -q -k "finalize or export"` 通过
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=apps/api uv run pytest apps/api/tests/test_finalize_session.py -q -k "finalize or export"</automated>
  </verify>
  <done>交付链路已经能提供 replay 所需的最小 finalize/export 里程碑线索，同时没有引入新的持久化模型。</done>
</task>

</tasks>

<verification>
整体验证必须证明：
- export 仍然复用 Phase 4 panel projection，而不是复制一份新的 section 拼装逻辑
- 导出内容保留章节与待验证项，且旧 `file_name/content` 契约不破坏
- finalize/export 为 replay 暴露了可识别里程碑，但没有新增持久化层
</verification>

<success_criteria>
用户确认后能得到结构化、可复制或下载的 PRD 文本，且交付链路与 review/panel contract 保持一致，为后续回放保留必要里程碑。
</success_criteria>

<output>
After completion, create `.planning/phases/05-zhi-liang-fu-he-yu-jiao-fu-hui-fang/05-02-SUMMARY.md`
</output>
