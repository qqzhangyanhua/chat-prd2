---
phase: 05-zhi-liang-fu-he-yu-jiao-fu-hui-fang
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/app/schemas/review.py
  - apps/api/app/schemas/session.py
  - apps/api/app/services/prd_review.py
  - apps/api/app/services/sessions.py
  - apps/api/tests/test_prd_review.py
  - apps/api/tests/test_sessions.py
autonomous: true
requirements:
  - RVW-01
must_haves:
  truths:
    - "会话快照返回独立于 `prd_snapshot` 的 `prd_review` 合同，而不是把质量检查塞回 panel contract"
    - "质量检查至少覆盖目标清晰度、范围边界、成功标准、风险暴露、待验证项完整度五个维度"
    - "质量检查结论由 persisted `prd_draft`、diagnostics、readiness 推导，不改写 PRD 真源"
  artifacts:
    - path: "apps/api/app/services/prd_review.py"
      provides: "独立 review projector"
    - path: "apps/api/app/schemas/review.py"
      provides: "review response schema"
    - path: "apps/api/app/services/sessions.py"
      provides: "snapshot 对外暴露 `prd_review`"
  key_links:
    - from: "apps/api/app/services/sessions.py"
      to: "apps/api/app/services/prd_review.py"
      via: "get_session_snapshot() 调用 review projector"
      pattern: "build_prd_review"
    - from: "apps/api/app/services/prd_review.py"
      to: "apps/api/app/agent/readiness.py"
      via: "复用 readiness / diagnostics 真源"
      pattern: "evaluate_finalize_readiness"
---

<objective>
定义 Phase 5 的 review contract：基于 persisted `prd_draft`、diagnostics 和 readiness 生成独立的 PRD 质量检查结果，并通过 session snapshot 暴露给前端。

Purpose: 先固定 review contract，避免把质量复核继续塞进 Phase 4 panel contract，保持“内容投影”和“质量判断”两层边界清楚。
Output: 独立 `prd_review` payload、snapshot schema 扩展、review contract 测试。
</objective>

<execution_context>
@/Users/zhangyanhua/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhangyanhua/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-zhi-liang-fu-he-yu-jiao-fu-hui-fang/05-RESEARCH.md
@.planning/phases/05-zhi-liang-fu-he-yu-jiao-fu-hui-fang/05-VALIDATION.md
@.planning/phases/04-prd-zeng-liang-bian-pai-yu-shou-lian-que-ren/04-01-SUMMARY.md
@.planning/phases/04-prd-zeng-liang-bian-pai-yu-shou-lian-que-ren/04-02-SUMMARY.md
@apps/api/app/services/prd_runtime.py
@apps/api/app/services/sessions.py
@apps/api/app/schemas/prd.py
@apps/api/app/schemas/session.py

<interfaces>
Current snapshot contract:

```python
class PrdSnapshotResponse(BaseModel):
    sections: dict[str, Any]
    meta: dict | None = None
    sections_changed: list[str] = Field(default_factory=list)
    missing_sections: list[str] = Field(default_factory=list)
    gap_prompts: list[str] = Field(default_factory=list)
    ready_for_confirmation: bool = False
```

```python
class SessionCreateResponse(BaseModel):
    session: SessionResponse
    state: StateSnapshot
    prd_snapshot: PrdSnapshotResponse
    messages: list[ConversationMessageResponse] = Field(default_factory=list)
    assistant_reply_groups: list[AssistantReplyGroupResponse] = Field(default_factory=list)
    turn_decisions: list[AgentTurnDecisionResponse] = Field(default_factory=list)
```

Existing boundary to preserve:

```python
def build_prd_updated_event_data(state: dict, state_patch: dict, prd_patch: dict) -> dict:
    ...
```

Do not extend `PrdUpdatedEventData` with review fields. Create a separate review schema and expose it beside `prd_snapshot`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 定义独立 review schema 和 projector，按五个质量维度输出 verdict/checklist</name>
  <read_first>
    .planning/phases/05-zhi-liang-fu-he-yu-jiao-fu-hui-fang/05-RESEARCH.md
    apps/api/app/services/prd_runtime.py
    apps/api/app/agent/readiness.py
    apps/api/app/services/message_state.py
    apps/api/tests/test_sessions.py
  </read_first>
  <files>apps/api/app/schemas/review.py, apps/api/app/services/prd_review.py, apps/api/tests/test_prd_review.py</files>
  <action>按本次规划假设实现独立 review contract，不污染 panel contract。具体要求：1) 新建 `apps/api/app/schemas/review.py` 与 `apps/api/app/services/prd_review.py`，review projector 必须独立于 `prd_runtime.py`，这是本 phase 的核心边界；2) review payload 至少包含总体 verdict / summary、逐维度 checks，以及缺口列表，五个维度必须覆盖 `goal_clarity`、`scope_boundary`、`success_metrics`、`risk_exposure`、`validation_completeness`；3) 评估输入只能来自 persisted `prd_draft.sections`、open diagnostics ledger、`evaluate_finalize_readiness()` 与 finalized 状态，禁止从 panel projection 反推质量判断；4) 对 `to_validate`、open risk、missing sections 给出明确降级逻辑，但不要把它们误写回 `prd_draft`；5) 测试需要覆盖“信息充分”“章节缺失”“只剩待验证项”“legacy snapshot fallback / 无结构化草稿”四类场景，保证 verdict 和 checks 可重复、可预测。</action>
  <acceptance_criteria>
    - `rg -n "goal_clarity|scope_boundary|success_metrics|risk_exposure|validation_completeness" apps/api/app/schemas/review.py apps/api/app/services/prd_review.py apps/api/tests/test_prd_review.py` 返回命中
    - `rg -n "evaluate_finalize_readiness|diagnostics|to_validate" apps/api/app/services/prd_review.py` 返回命中
    - `PYTHONPATH=apps/api uv run pytest apps/api/tests/test_prd_review.py -q` 通过
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=apps/api uv run pytest apps/api/tests/test_prd_review.py -q</automated>
  </verify>
  <done>后端已经有独立 review projector，可以稳定回答“当前 PRD 质量如何”，而不会混淆为“当前 PRD 长什么样”。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 通过 session snapshot 暴露 `prd_review`，保持 review contract 与 panel contract 解耦</name>
  <read_first>
    .planning/phases/05-zhi-liang-fu-he-yu-jiao-fu-hui-fang/05-RESEARCH.md
    apps/api/app/schemas/session.py
    apps/api/app/services/sessions.py
    apps/api/tests/test_sessions.py
  </read_first>
  <files>apps/api/app/schemas/session.py, apps/api/app/services/sessions.py, apps/api/tests/test_sessions.py</files>
  <action>把 review contract 接到 snapshot 出口，但不要把它塞进 `prd_snapshot`。具体要求：1) `SessionCreateResponse` 新增单独的 `prd_review` 字段，保持 `prd_snapshot` 继续只承载 Phase 4 panel projection；2) `get_session_snapshot()` 在构建 `messages / assistant_reply_groups / turn_decisions / prd_snapshot` 后，再调用 review projector 产出 `prd_review`，避免反向依赖 panel payload；3) 对 legacy session 或空草稿仍要返回结构稳定的 review payload，至少给出 `missing` / `needs_input` 语义，而不是缺字段；4) 回归测试必须锁定 snapshot 同时返回 `prd_snapshot` 与 `prd_review`，且 review 维度与 section 缺口、open diagnostics 保持一致；5) 这里不要新增独立 review route，先走 snapshot 复用链路即可。</action>
  <acceptance_criteria>
    - `rg -n "prd_review" apps/api/app/schemas/session.py apps/api/app/services/sessions.py apps/api/tests/test_sessions.py` 返回命中
    - `rg -n "prd_snapshot" apps/api/app/schemas/session.py apps/api/tests/test_sessions.py` 仍然返回命中，证明 panel contract 未被替换
    - `PYTHONPATH=apps/api uv run pytest apps/api/tests/test_sessions.py -q -k "review or snapshot"` 通过
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=apps/api uv run pytest apps/api/tests/test_sessions.py -q -k "review or snapshot"</automated>
  </verify>
  <done>会话快照对外同时暴露 panel projection 和 review contract，两者边界清楚，后续 export 与前端消费都能直接复用。</done>
</task>

</tasks>

<verification>
整体验证必须证明三件事：
- `prd_review` 是 snapshot 的 sibling 字段，而不是 `prd_snapshot` 的补丁字段
- 五个 review 维度都能从 persisted truth 稳定得出，不依赖前端推断
- legacy / 缺失数据场景仍返回结构稳定的 review payload
</verification>

<success_criteria>
Phase 5 的 review contract 完成后，系统已经能对当前 PRD 给出结构化质量判断，而且这套判断与 Phase 4 panel projection 保持清晰解耦。
</success_criteria>

<output>
After completion, create `.planning/phases/05-zhi-liang-fu-he-yu-jiao-fu-hui-fang/05-01-SUMMARY.md`
</output>
