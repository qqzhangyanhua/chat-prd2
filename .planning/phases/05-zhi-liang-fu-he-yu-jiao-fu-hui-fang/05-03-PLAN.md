---
phase: 05-zhi-liang-fu-he-yu-jiao-fu-hui-fang
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - apps/api/app/schemas/replay.py
  - apps/api/app/schemas/session.py
  - apps/api/app/services/session_replay.py
  - apps/api/app/services/sessions.py
  - apps/api/tests/test_sessions.py
  - apps/web/src/lib/types.ts
  - apps/web/src/store/workspace-store.ts
  - apps/web/src/components/workspace/prd-panel.tsx
  - apps/web/src/components/workspace/replay-panel.tsx
  - apps/web/src/components/workspace/workspace-session-shell.tsx
  - apps/web/src/test/workspace-store.test.ts
  - apps/web/src/test/prd-panel.test.tsx
  - apps/web/src/test/replay-panel.test.tsx
  - apps/web/src/test/workspace-session-shell.test.tsx
autonomous: true
requirements:
  - RVW-01
  - RVW-03
must_haves:
  truths:
    - "用户查看当前 PRD 时能看到独立 review summary，而不是把 panel contract 改造成审查合同"
    - "用户回放单个会话时，能按时间线看到 guidance decision、diagnostics、PRD 变化，以及 finalize/export 里程碑"
    - "回放基于现有 `messages + assistant_reply_groups + turn_decisions + state/prd versions` 聚合，不引入新持久化层"
  artifacts:
    - path: "apps/api/app/services/session_replay.py"
      provides: "单会话 replay timeline 聚合"
    - path: "apps/web/src/components/workspace/replay-panel.tsx"
      provides: "前端 replay timeline 展示"
    - path: "apps/web/src/store/workspace-store.ts"
      provides: "snapshot hydrate 后的 review/replay state"
  key_links:
    - from: "apps/api/app/services/session_replay.py"
      to: "apps/api/app/services/sessions.py"
      via: "get_session_snapshot() 注入 replay timeline"
      pattern: "build_session_replay_timeline"
    - from: "apps/web/src/store/workspace-store.ts"
      to: "apps/web/src/components/workspace/replay-panel.tsx"
      via: "hydrateSession()/refreshSessionSnapshot()"
      pattern: "replayTimeline"
---

<objective>
聚合单会话 replay timeline，并在前端消费 `prd_review` 与 `replay_timeline`：用户既能看到当前 PRD 的基础质量反馈，也能回放“为什么变成现在这样”。

Purpose: 完成 Phase 5 的用户可见交付，不新增持久化系统，只把已有 messages / turn decisions / snapshot versions 组织成可读回放。
Output: 后端 replay timeline 聚合、前端 review/replay 消费、回归测试。
</objective>

<execution_context>
@/Users/zhangyanhua/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhangyanhua/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-zhi-liang-fu-he-yu-jiao-fu-hui-fang/05-RESEARCH.md
@.planning/phases/05-zhi-liang-fu-he-yu-jiao-fu-hui-fang/05-01-PLAN.md
@.planning/phases/05-zhi-liang-fu-he-yu-jiao-fu-hui-fang/05-02-PLAN.md
@apps/api/app/services/sessions.py
@apps/web/src/store/workspace-store.ts
@apps/web/src/components/workspace/prd-panel.tsx
@apps/web/src/components/workspace/workspace-session-shell.tsx

<interfaces>
Current frontend snapshot type:

```ts
export interface SessionSnapshotResponse {
  session: SessionResponse;
  state: StateSnapshotResponse;
  prd_snapshot: {
    sections: Record<string, Record<string, unknown>>;
    meta?: PrdMeta;
    sections_changed?: string[];
    missing_sections?: string[];
    gap_prompts?: string[];
    ready_for_confirmation?: boolean;
  };
  messages: ConversationMessage[];
  assistant_reply_groups?: AssistantReplyGroup[];
  turn_decisions?: AgentTurnDecision[];
}
```

Current replay raw materials:

```python
assistant_reply_groups = _list_assistant_reply_groups(db, session_id)
messages = _build_timeline_messages(raw_messages, assistant_reply_groups)
turn_decisions = _list_turn_decisions(db, session_id)
```

Build replay from these existing records plus finalized/export milestones from Plan 05-02. Do not introduce new tables.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 在后端聚合单会话 replay timeline，并把 review/replay 一起接入 snapshot</name>
  <read_first>
    .planning/phases/05-zhi-liang-fu-he-yu-jiao-fu-hui-fang/05-RESEARCH.md
    apps/api/app/services/sessions.py
    apps/api/app/schemas/session.py
    apps/api/tests/test_sessions.py
  </read_first>
  <files>apps/api/app/schemas/replay.py, apps/api/app/schemas/session.py, apps/api/app/services/session_replay.py, apps/api/app/services/sessions.py, apps/api/tests/test_sessions.py</files>
  <action>实现单会话 replay timeline 聚合，严格遵守本次运行边界。具体要求：1) 新建 `apps/api/app/services/session_replay.py` 与 `apps/api/app/schemas/replay.py`，聚合输入只能来自现有 `messages`、`assistant_reply_groups`、`turn_decisions`、最新/历史 state 或 prd snapshot 版本，以及 Plan 05-02 暴露出的 finalize/export 里程碑；2) timeline item 至少区分 guidance、diagnostics、prd_delta、finalize、export 五类事件，但 Phase 5 MVP 只做 narrative-first 聚合，不要求精确 persisted diff viewer；3) `SessionCreateResponse` 新增 `replay_timeline` 字段，并继续返回 `prd_review`，保证 snapshot 一次 hydrate 就有 review + replay；4) 严禁新增 repository、DB model、migration 或持久化表，`rg -n "timeline_events|review_events|ReplayEvent" apps/api/app` 不应出现新的 ORM/迁移实现；5) 测试要覆盖多轮会话聚合顺序、单条消息对应的 guidance/diagnostics 摘要、PRD 变化摘要，以及 finalize/export 里程碑出现在 timeline 尾部。</action>
  <acceptance_criteria>
    - `rg -n "guidance|diagnostics|prd_delta|finalize|export" apps/api/app/schemas/replay.py apps/api/app/services/session_replay.py apps/api/tests/test_sessions.py` 返回命中
    - `rg -n "replay_timeline|prd_review" apps/api/app/schemas/session.py apps/api/app/services/sessions.py apps/api/tests/test_sessions.py` 返回命中
    - `rg -n "timeline_events|review_events" apps/api/app/db apps/api/alembic` 无命中
    - `PYTHONPATH=apps/api uv run pytest apps/api/tests/test_sessions.py -q -k "replay or timeline or review"` 通过
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=apps/api uv run pytest apps/api/tests/test_sessions.py -q -k "replay or timeline or review"</automated>
  </verify>
  <done>session snapshot 已能返回 review summary 和 narrative-first replay timeline，且整个实现没有引入新的持久化层。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 在前端消费 review/replay contract，保持 review 与 panel contract 分层</name>
  <read_first>
    .planning/phases/05-zhi-liang-fu-he-yu-jiao-fu-hui-fang/05-01-PLAN.md
    .planning/phases/05-zhi-liang-fu-he-yu-jiao-fu-hui-fang/05-02-PLAN.md
    apps/web/src/lib/types.ts
    apps/web/src/store/workspace-store.ts
    apps/web/src/components/workspace/prd-panel.tsx
    apps/web/src/components/workspace/workspace-session-shell.tsx
    apps/web/src/test/workspace-store.test.ts
  </read_first>
  <files>apps/web/src/lib/types.ts, apps/web/src/store/workspace-store.ts, apps/web/src/components/workspace/prd-panel.tsx, apps/web/src/components/workspace/replay-panel.tsx, apps/web/src/components/workspace/workspace-session-shell.tsx, apps/web/src/test/workspace-store.test.ts, apps/web/src/test/prd-panel.test.tsx, apps/web/src/test/replay-panel.test.tsx, apps/web/src/test/workspace-session-shell.test.tsx</files>
  <action>把 review/replay 合同交付给用户，但不要破坏既有 panel 边界。具体要求：1) 扩展 `SessionSnapshotResponse` 和 store state，独立保存 `prdReview` 与 `replayTimeline`，不要把 review 字段并回 `prd` state；2) `PrdPanel` 可以展示一个 review summary 区块，用来满足 RVW-01，但正文 section 仍只读 `prd.sections`，这是“review contract 独立于 panel contract”的前端体现；3) 新建 `ReplayPanel`，展示单会话 timeline，至少能让用户看到 guidance decision、diagnostics、PRD change、finalize/export milestone；4) `workspace-session-shell` 负责把 replay UI 放进合适位置，但不要把 Phase 3 first-draft / diagnostics ledger 从会话列挪走；5) 测试必须覆盖 snapshot hydrate、页面刷新后一致性、review summary 不污染 panel sections、timeline 渲染顺序以及 finalize/export milestone 的展示。</action>
  <acceptance_criteria>
    - `rg -n "prdReview|replayTimeline" apps/web/src/lib/types.ts apps/web/src/store/workspace-store.ts apps/web/src/components/workspace/replay-panel.tsx` 返回命中
    - `rg -n "review|质量|Replay|timeline|finalize|export" apps/web/src/components/workspace/prd-panel.tsx apps/web/src/components/workspace/replay-panel.tsx apps/web/src/test/prd-panel.test.tsx apps/web/src/test/replay-panel.test.tsx` 返回命中
    - `pnpm --filter web test -- src/test/workspace-store.test.ts src/test/prd-panel.test.tsx src/test/replay-panel.test.tsx src/test/workspace-session-shell.test.tsx` 通过
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter web test -- src/test/workspace-store.test.ts src/test/prd-panel.test.tsx src/test/replay-panel.test.tsx src/test/workspace-session-shell.test.tsx</automated>
  </verify>
  <done>用户在工作台里既能看到当前 PRD 的基础质量反馈，也能回放单会话的关键决策与变更过程，而且 review 与 panel 仍然是两份独立合同。</done>
</task>

</tasks>

<verification>
整体验证必须证明：
- 前端把 `prdReview` 与 `prd` 分开维护，review summary 不会污染 panel sections
- replay 只基于现有 snapshot 原料聚合，没有新表或新持久化层
- timeline 能解释“为什么 PRD 变成现在这样”，而不是只重复最终文档内容
</verification>

<success_criteria>
Phase 5 完成后，用户在同一个工作台里既能获得当前 PRD 的基础质量反馈，也能通过 replay timeline 回看 guidance、诊断、PRD 变化和交付里程碑。
</success_criteria>

<output>
After completion, create `.planning/phases/05-zhi-liang-fu-he-yu-jiao-fu-hui-fang/05-03-SUMMARY.md`
</output>
