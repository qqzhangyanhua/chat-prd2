---
phase: 02-zhen-duan-shen-wa-yu-wen-ti-tai-zhang
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/app/agent/types.py
  - apps/api/app/agent/pm_mentor.py
  - apps/api/app/agent/runtime.py
  - apps/api/app/services/message_state.py
  - apps/api/tests/test_pm_mentor.py
  - apps/api/tests/test_agent_runtime.py
autonomous: true
requirements:
  - DIAG-01
  - DIAG-02
must_haves:
  truths:
    - 当用户前后说法冲突、缺少关键信息或带着未确认前提推进时，后端会输出结构化诊断项，而不是只留下自由文本 `challenge`
    - 每个诊断项都明确标出 `type`、`bucket`、`impact_scope`、`suggested_next_step` 和 `status`，前后端都不需要再猜字段含义
    - 探索态输入不会被粗暴归类为矛盾；只有命中明确规则时才生成 `contradiction`
  artifacts:
    - path: apps/api/app/agent/types.py
      provides: DiagnosticItem / DiagnosticSummary / TurnDecision diagnostics 契约
      contains: class TurnDecision
    - path: apps/api/app/agent/pm_mentor.py
      provides: contradiction / gap / assumption 识别与去重规则
      contains: run_pm_mentor
    - path: apps/api/app/services/message_state.py
      provides: diagnostics state patch 归一化与兼容派生字段映射
      contains: build_decision_state_patch
  key_links:
    - from: apps/api/app/agent/pm_mentor.py
      to: apps/api/app/agent/types.py
      via: TurnDecision.diagnostics
      pattern: DiagnosticItem|diagnostics
    - from: apps/api/app/agent/runtime.py
      to: apps/api/app/services/message_state.py
      via: local fallback / greeting / completed 路径的空 diagnostics contract
      pattern: diagnostics|diagnostic_summary
    - from: apps/api/app/services/message_state.py
      to: apps/api/app/schemas/state.py
      via: state_patch diagnostics fields
      pattern: diagnostic_summary|diagnostics
---

<objective>
为 Phase 2 建立后端诊断真源：把“矛盾 / 缺口 / 假设”从零散文本和旧字段升级成统一的 `DiagnosticItem` 契约，并在 `TurnDecision` 与 state patch 中稳定输出。

Purpose: 只有先把诊断项 contract 和识别规则做成一等结构，后续的持久化、SSE、snapshot 和前端台账才能复用同一份真相。
Output: 诊断契约、误报受控的检测规则、兼容现有 guidance 链路的后端自动化测试。
</objective>

<execution_context>
@/Users/zhangyanhua/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhangyanhua/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-zhen-duan-shen-wa-yu-wen-ti-tai-zhang/02-RESEARCH.md
@.planning/phases/01-yin-dao-jie-zou-yu-xuan-xiang-cheng-qing/01-01-SUMMARY.md
@.planning/phases/01-yin-dao-jie-zou-yu-xuan-xiang-cheng-qing/01-02-SUMMARY.md
@apps/api/app/agent/types.py
@apps/api/app/agent/pm_mentor.py
@apps/api/app/agent/runtime.py
@apps/api/app/services/message_state.py

<interfaces>
From apps/api/app/agent/types.py:
```python
class TurnDecision:
    assumptions: list[dict[str, Any]]
    gaps: list[str]
    challenges: list[str]
    pm_risk_flags: list[str]
    state_patch: dict[str, Any]
```

From apps/api/app/services/message_state.py:
```python
def build_decision_state_patch(turn_decision: object) -> dict:
    return {
        "working_hypotheses": assumptions,
        "pm_risk_flags": ...,
        "next_best_questions": ...,
    }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 定义统一 diagnostics 契约并让所有运行路径返回同形结构</name>
  <read_first>
    .planning/phases/02-zhen-duan-shen-wa-yu-wen-ti-tai-zhang/02-RESEARCH.md
    .planning/phases/01-yin-dao-jie-zou-yu-xuan-xiang-cheng-qing/01-01-SUMMARY.md
    apps/api/app/agent/types.py
    apps/api/app/agent/runtime.py
    apps/api/app/services/message_state.py
    apps/api/tests/test_agent_runtime.py
    apps/api/tests/test_message_state.py
  </read_first>
  <files>apps/api/app/agent/types.py, apps/api/app/agent/runtime.py, apps/api/app/services/message_state.py, apps/api/tests/test_agent_runtime.py, apps/api/tests/test_message_state.py</files>
  <behavior>
    - Runtime shape Test 1: `TurnDecision` 支持 `diagnostics` 与 `diagnostic_summary`，且每个 item 至少包含 `id`、`type`、`bucket`、`status`、`title`、`detail`、`impact_scope`、`suggested_next_step`
    - Runtime shape Test 2: greeting / completed / fallback 等本地路径即使没有诊断项，也返回空数组和零计数 summary，而不是缺字段
    - State patch Test 1: `build_decision_state_patch()` 会把 `diagnostics`、`diagnostic_summary` 写入 state patch，且不依赖 SSE/schema 层补默认值
    - State patch Test 2: `build_decision_state_patch()` 会同时派生兼容字段 `working_hypotheses`、`pm_risk_flags`、`open_questions`，并对 `suggested_next_step.prompt` 去重
  </behavior>
  <action>先把 diagnostics contract 定死，再允许检测逻辑接入。具体要求：1) 在 `apps/api/app/agent/types.py` 新增用于本 phase 的 typed contract，至少包括 `DiagnosticType = contradiction|gap|assumption`、`DiagnosticBucket = unknown|risk|to_validate`、`DiagnosticStatus = open|resolved|superseded`、`DiagnosticItem`、`DiagnosticSummary`；`DiagnosticItem.suggested_next_step` 必须是对象而不是纯字符串，并至少含 `action_kind`、`label`、`prompt`；2) 在 `TurnDecision` 新增 `diagnostics: list[DiagnosticItem]` 与 `diagnostic_summary: dict[str, Any]` 字段，并保留旧字段 `assumptions / gaps / challenges / pm_risk_flags` 作为兼容派生来源，不要删除；3) 在 `runtime.py` 的 greeting、completed、finalize fallback、本地降级回复等路径统一填入空 diagnostics contract，保证后续持久化/SSE 不需要判空补结构；4) 在 `message_state.py` 新增 diagnostics 归一化 helper，把 `TurnDecision.diagnostics` 写入 `state_patch["diagnostics"]`，把 summary 写入 `state_patch["diagnostic_summary"]`，同时按研究结论生成兼容派生字段：`working_hypotheses` 来自 `type=assumption`，`pm_risk_flags` 来自 `bucket=risk`，`open_questions` 来自 `suggested_next_step.prompt` 的去重列表；5) 不要在这一任务改动 `schemas/message.py` 或 session API，SSE/schema 透出留给 02-02。</action>
  <acceptance_criteria>
    - `rg -n "DiagnosticItem|DiagnosticSummary|diagnostics|diagnostic_summary" apps/api/app/agent/types.py apps/api/app/agent/runtime.py apps/api/app/services/message_state.py` 返回命中
    - `rg -n "\"diagnostics\"|\"diagnostic_summary\"|\"open_questions\"|\"working_hypotheses\"|\"pm_risk_flags\"" apps/api/app/services/message_state.py` 返回命中
    - `PYTHONPATH=apps/api uv run pytest apps/api/tests/test_agent_runtime.py apps/api/tests/test_message_state.py -q -k "diagnostic"` 通过
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=apps/api uv run pytest apps/api/tests/test_agent_runtime.py apps/api/tests/test_message_state.py -q -k "diagnostic"</automated>
  </verify>
  <done>所有 `TurnDecision` 生成路径都具备一致的 diagnostics shape，后续层不再需要自己猜哪些字段缺失。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 在 pm_mentor 中实现误报受控的 contradiction / gap / assumption 识别规则</name>
  <read_first>
    .planning/phases/02-zhen-duan-shen-wa-yu-wen-ti-tai-zhang/02-RESEARCH.md
    apps/api/app/agent/pm_mentor.py
    apps/api/app/agent/types.py
    apps/api/app/services/message_state.py
    apps/api/tests/test_pm_mentor.py
  </read_first>
  <files>apps/api/app/agent/pm_mentor.py, apps/api/app/agent/types.py, apps/api/app/services/message_state.py, apps/api/tests/test_pm_mentor.py</files>
  <behavior>
    - Test 1: 当用户显式推翻已有 `target_user`、`problem`、`solution`、`mvp_scope` 中的已知值时，会生成 `type=contradiction` 的 open item，并带上对应 `impact_scope`
    - Test 2: 当关键维度缺失时，会生成 `type=gap` 的 open item，并给出可直接提问的 `suggested_next_step.prompt`
    - Test 3: 当用户依赖未确认前提推进时，会生成 `type=assumption` 的 open item；探索态表述不会仅因为模糊就被误判成 `contradiction`
  </behavior>
  <action>把研究里建议的检测逻辑固定在 `pm_mentor.py`，不要让前端或 session 层重复判断。具体要求：1) 基于现有 `_has_contradictory_info()`、`_is_jumping_around()` 等 helper 扩展出专用 diagnostics builder，例如 `_collect_diagnostic_items()`，其输入必须包含当前 `state`、用户输入、LLM 输出和当前 focus；2) `contradiction` 仅在“state 已存在非空 canonical 值 + 当前输入明确否定/替换该值”时创建，至少覆盖 `target_user`、`problem`、`solution`、`mvp_scope` 四个维度，禁止把“还没想清楚”“也可能”这类探索态文本标成矛盾；3) `gap` 至少覆盖“目标用户缺失、核心问题缺失、方案缺失、验证标准缺失”四类缺口，并把 `suggested_next_step.action_kind` 固定成 `ask_user`，`prompt` 使用可直接追问的一句话；4) `assumption` 用于表达当前回复要继续推进所依赖但用户尚未确认的前提，例如“默认面向独立开发者”“默认先做 Web 端”；5) 为每个 item 生成 session 内稳定 id，推荐由 `type + impact_scope + normalized_title` 组成，去重规则必须确保同轮同义项只保留一条 open item；6) 保持对旧字段的兼容：`TurnDecision.assumptions` 仅保留 assumption 子集摘要，`gaps` 保留 gap 标题列表，`challenges` 只在存在 contradiction 或高风险 assumption 时生成用户可读一句话。</action>
  <acceptance_criteria>
    - `rg -n "contradiction|gap|assumption|impact_scope|suggested_next_step" apps/api/app/agent/pm_mentor.py apps/api/tests/test_pm_mentor.py` 返回命中
    - `rg -n "_has_contradictory_info|diagnostic" apps/api/app/agent/pm_mentor.py` 返回命中，且 diagnostics builder 只出现在 agent 层
    - `PYTHONPATH=apps/api uv run pytest apps/api/tests/test_pm_mentor.py -q -k "diagnostic"` 通过
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=apps/api uv run pytest apps/api/tests/test_pm_mentor.py -q -k "diagnostic"</automated>
  </verify>
  <done>`pm_mentor` 可以稳定产出结构化诊断项，并用规则控制矛盾误报，不再依赖自由文本 `challenge` 承载诊断。</done>
</task>

</tasks>

<verification>
后端验证必须同时覆盖：
- `DiagnosticItem` 契约字段齐全且所有路径返回同形结构
- `contradiction / gap / assumption` 的分类规则可由测试直接断言
- 兼容字段 `working_hypotheses / pm_risk_flags / open_questions` 仍能从 diagnostics 派生
</verification>

<success_criteria>
Phase 2 的诊断逻辑真源固定在 agent 层后，后续持久化、SSE、snapshot 和 UI 只需要消费 `diagnostics` 与 `diagnostic_summary`，不再从 `challenge`、`risk_flags` 等旧字段反推诊断意图。
</success_criteria>

<output>
After completion, create `.planning/phases/02-zhen-duan-shen-wa-yu-wen-ti-tai-zhang/02-01-SUMMARY.md`
</output>
