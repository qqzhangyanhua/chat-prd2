---
phase: 02-zhen-duan-shen-wa-yu-wen-ti-tai-zhang
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - apps/web/src/lib/types.ts
  - apps/web/src/store/workspace-store.ts
  - apps/web/src/components/workspace/assistant-turn-card.tsx
  - apps/web/src/components/workspace/workspace-session-shell.tsx
  - apps/web/src/components/workspace/diagnostics-ledger-card.tsx
  - apps/web/src/test/workspace-store.test.ts
  - apps/web/src/test/assistant-turn-card.test.tsx
  - apps/web/src/test/workspace-session-shell.test.tsx
autonomous: true
requirements:
  - DIAG-02
  - DIAG-03
must_haves:
  truths:
    - 用户在会话列里能看到“本轮诊断”摘要，知道这轮暴露的是矛盾、缺口还是假设
    - 用户在会话过程中持续看到未知项 / 风险 / 待验证清单，不需要切到 PRD 面板
    - 刷新页面后，诊断台账仍能从 session snapshot 恢复
  artifacts:
    - path: apps/web/src/lib/types.ts
      provides: diagnostics / ledger 前端共享类型
      contains: interface DecisionDiagnosticItem
    - path: apps/web/src/store/workspace-store.ts
      provides: diagnostics hydrate / SSE merge 真源
      contains: hydrateSession
    - path: apps/web/src/components/workspace/diagnostics-ledger-card.tsx
      provides: 会话列 open ledger UI
      min_lines: 60
  key_links:
    - from: apps/web/src/lib/types.ts
      to: apps/web/src/store/workspace-store.ts
      via: DecisionReadyData / StateSnapshotResponse diagnostics fields
      pattern: diagnostics|diagnosticSummary
    - from: apps/web/src/store/workspace-store.ts
      to: apps/web/src/components/workspace/assistant-turn-card.tsx
      via: latest turn diagnostics summary
      pattern: latestDiagnostics|diagnosticSummary
    - from: apps/web/src/store/workspace-store.ts
      to: apps/web/src/components/workspace/diagnostics-ledger-card.tsx
      via: open ledger groups
      pattern: unknown|risk|toValidate
---

<objective>
把 Phase 2 的 diagnostics ledger 以最小可交付形式落到工作台会话列：展示“本轮诊断摘要”和“持续问题台账”，但不提前侵入右侧 PRD 面板。

Purpose: 满足 DIAG-03 的“过程持续可见”，同时严格守住 Phase 2 的 UI 边界，不把缺口可视化提前做成 Phase 4 的 PRD 编排。
Output: 前端共享类型、workspace store diagnostics 派生、会话列最小诊断卡片与 Vitest 回归。
</objective>

<execution_context>
@/Users/zhangyanhua/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhangyanhua/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-zhen-duan-shen-wa-yu-wen-ti-tai-zhang/02-RESEARCH.md
@.planning/phases/02-zhen-duan-shen-wa-yu-wen-ti-tai-zhang/02-02-PLAN.md
@.planning/phases/01-yin-dao-jie-zou-yu-xuan-xiang-cheng-qing/01-03-SUMMARY.md
@apps/web/src/lib/types.ts
@apps/web/src/store/workspace-store.ts
@apps/web/src/components/workspace/assistant-turn-card.tsx
@apps/web/src/components/workspace/workspace-session-shell.tsx

<interfaces>
From apps/web/src/lib/types.ts:
```typescript
export interface StateSnapshotResponse {
  response_mode?: GuidanceResponseMode;
  guidance_mode?: GuidanceMode;
  guidance_step?: GuidanceStep;
  focus_dimension?: string | null;
}

export interface DecisionGuidance {
  strategyLabel: string;
  guidanceMode: GuidanceMode | null;
  guidanceStep: GuidanceStep | null;
  focusDimension: string | null;
}
```

From apps/web/src/store/workspace-store.ts:
```typescript
interface WorkspaceState {
  decisionGuidance: DecisionGuidance | null;
  hydrateSession: (snapshot: SessionSnapshotResponse) => void;
  applyEvent: (event: WorkspaceEvent) => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展前端 diagnostics 类型并让 store 统一消费 snapshot + decision.ready</name>
  <read_first>
    .planning/phases/02-zhen-duan-shen-wa-yu-wen-ti-tai-zhang/02-RESEARCH.md
    .planning/phases/01-yin-dao-jie-zou-yu-xuan-xiang-cheng-qing/01-03-SUMMARY.md
    apps/web/src/lib/types.ts
    apps/web/src/store/workspace-store.ts
    apps/web/src/test/workspace-store.test.ts
  </read_first>
  <files>apps/web/src/lib/types.ts, apps/web/src/store/workspace-store.ts, apps/web/src/test/workspace-store.test.ts</files>
  <behavior>
    - Test 1: `hydrateSession()` 能把 `state.diagnostics` 和 `state.diagnostic_summary` 转成前端可用的 open ledger
    - Test 2: `decision.ready` 事件会更新“本轮 diagnostics”和 ledger summary，但不会清空已有 open ledger
    - Test 3: 当 snapshot 比当前 PRD/诊断状态更旧时，store 仍遵守现有 stale snapshot 保护，不回滚到旧 ledger
  </behavior>
  <action>把 diagnostics 纳入前端单一真相源，但保持与 Phase 1 guidance 同样的“store 负责归一化、组件只渲染”模式。具体要求：1) 在 `types.ts` 新增 diagnostics 相关类型，至少包括 `DecisionDiagnosticItem`、`DecisionDiagnosticSummary`、`DiagnosticLedgerGroup`，并为 `DecisionReadyData`、`StateSnapshotResponse`、`AgentTurnDecisionSectionMeta` 补上 `diagnostics`、`diagnostic_summary`、`ledger_summary` 字段；2) 在 `workspace-store.ts` 新增 diagnostics normalize helpers，把后端 snake_case 转成前端 camelCase，至少保留 `type`、`bucket`、`status`、`impactScope`、`suggestedNextStep`、`confidence`；3) store 中新增“最新一轮 diagnostics”和“open ledger summary”状态，并明确：session hydrate 读 snapshot open ledger，`decision.ready` 读本轮 diagnostics + ledger summary，二者共用同一套 normalize helper；4) 继续保留 Phase 1 的 stale snapshot 保护逻辑，不允许刷新较旧 snapshot 时覆盖当前更完整的 diagnostics ledger；5) 测试只覆盖会话列相关状态，不得改 `PrdPanel` 或 `prd-store-helpers.ts`。</action>
  <acceptance_criteria>
    - `rg -n "DecisionDiagnostic|diagnosticSummary|ledgerSummary|suggestedNextStep" apps/web/src/lib/types.ts apps/web/src/store/workspace-store.ts` 返回命中
    - `rg -n "diagnostic|ledger|stale snapshot|decision.ready" apps/web/src/test/workspace-store.test.ts` 返回命中
    - `cd apps/web && pnpm test -- src/test/workspace-store.test.ts -t diagnostic` 通过
  </acceptance_criteria>
  <verify>
    <automated>cd apps/web && pnpm test -- src/test/workspace-store.test.ts -t diagnostic</automated>
  </verify>
  <done>workspace store 成为 diagnostics 的唯一前端真源，SSE 与 snapshot 两条路径的诊断状态不再分叉。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 在会话列渲染本轮诊断摘要与持续问题台账，且不触碰 PRD 面板</name>
  <read_first>
    .planning/phases/02-zhen-duan-shen-wa-yu-wen-ti-tai-zhang/02-RESEARCH.md
    apps/web/src/components/workspace/assistant-turn-card.tsx
    apps/web/src/components/workspace/workspace-session-shell.tsx
    apps/web/src/test/assistant-turn-card.test.tsx
    apps/web/src/test/workspace-session-shell.test.tsx
  </read_first>
  <files>apps/web/src/components/workspace/assistant-turn-card.tsx, apps/web/src/components/workspace/workspace-session-shell.tsx, apps/web/src/components/workspace/diagnostics-ledger-card.tsx, apps/web/src/test/assistant-turn-card.test.tsx, apps/web/src/test/workspace-session-shell.test.tsx</files>
  <behavior>
    - Test 1: `AssistantTurnCard` 展示本轮 diagnostics 摘要，至少可见 `type`、`impact_scope` 和 `suggested_next_step.label`
    - Test 2: 新增 `DiagnosticsLedgerCard` 在会话列按 `unknown / risk / to_validate` 三组展示 open 项，刷新 hydrate 后仍可见
    - Test 3: 右侧 PRD 面板完全不变；诊断 UI 只出现在 conversation column
  </behavior>
  <action>把诊断 UI 控制在会话列最小范围内。具体要求：1) 在 `assistant-turn-card.tsx` 的 guidance 区后新增“本轮诊断”块，最多展示本轮最重要的 3 条 item，文案必须直接显示类型中文标签（矛盾 / 缺口 / 假设）、影响范围和建议下一步动作，不要只显示红黄标签；2) 新建 `diagnostics-ledger-card.tsx`，按 bucket 分成“未知项”“风险”“待验证”三组，只显示 `status=open` 项，每项至少展示 title、detail、impact scope pills 和 suggested next step prompt；3) 在 `workspace-session-shell.tsx` 的 conversation column 中，把 `DiagnosticsLedgerCard` 放在 `ConversationPanel` 下方或同列区域，明确不修改 `PrdPanel` 引用，也不向 PRD column 传 diagnostics props；4) 若某一 bucket 没有 open 项，组件显示“当前无此类问题”而不是整个卡片消失，确保用户知道 ledger 是持续维护的；5) 为 `assistant-turn-card.test.tsx` 与 `workspace-session-shell.test.tsx` 增加断言，确认会话列能看到 diagnostics 内容、hydrate 后恢复、且 DOM 中不存在新增 PRD 面板 diagnostics 标记。</action>
  <acceptance_criteria>
    - `test -f apps/web/src/components/workspace/diagnostics-ledger-card.tsx` 成功
    - `rg -n "矛盾|缺口|假设|未知项|待验证|风险" apps/web/src/components/workspace/assistant-turn-card.tsx apps/web/src/components/workspace/diagnostics-ledger-card.tsx` 返回命中
    - `rg -n "diagnostic|ledger|conversation column|PrdPanel" apps/web/src/test/assistant-turn-card.test.tsx apps/web/src/test/workspace-session-shell.test.tsx` 返回命中
    - `cd apps/web && pnpm test -- src/test/assistant-turn-card.test.tsx src/test/workspace-session-shell.test.tsx -t diagnostic` 通过
  </acceptance_criteria>
  <verify>
    <automated>cd apps/web && pnpm test -- src/test/assistant-turn-card.test.tsx src/test/workspace-session-shell.test.tsx -t diagnostic</automated>
  </verify>
  <done>用户只在会话列就能看到本轮诊断与持续问题台账，且没有把 diagnostics UI 提前塞进右侧 PRD 面板。</done>
</task>

</tasks>

<verification>
前端验证必须同时覆盖：
- snapshot hydrate 与 `decision.ready` 都能恢复 diagnostics
- 会话列可见“本轮诊断”与“持续台账”两层信息
- PRD 面板未被 Phase 2 改造，诊断 UI 没有越界到右侧列
</verification>

<success_criteria>
Phase 2 的最小 UI 完成后，用户在 conversation column 内就能持续看到当前未知项 / 风险 / 待验证清单，并理解每个问题为什么重要、下一步该补什么信息。
</success_criteria>

<output>
After completion, create `.planning/phases/02-zhen-duan-shen-wa-yu-wen-ti-tai-zhang/02-03-SUMMARY.md`
</output>
