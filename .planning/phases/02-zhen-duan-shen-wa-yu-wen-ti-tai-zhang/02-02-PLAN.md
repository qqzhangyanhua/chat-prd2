---
phase: 02-zhen-duan-shen-wa-yu-wen-ti-tai-zhang
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - apps/api/app/repositories/agent_turn_decisions.py
  - apps/api/app/schemas/message.py
  - apps/api/app/schemas/state.py
  - apps/api/app/services/message_state.py
  - apps/api/app/services/sessions.py
  - apps/api/tests/test_messages_stream.py
  - apps/api/tests/test_sessions.py
autonomous: true
requirements:
  - DIAG-02
  - DIAG-03
must_haves:
  truths:
    - 本轮诊断项会通过 `decision.ready` 和 session snapshot 一致地暴露给前端，而不是只在内存里短暂存在
    - 刷新会话后，仍处于 open 状态的未知项 / 风险 / 待验证项会恢复出来
    - 每个 open ledger 都能按 `unknown / risk / to_validate` 统计摘要，供前端直接渲染
  artifacts:
    - path: apps/api/app/schemas/message.py
      provides: decision.ready diagnostics / ledger summary 输出契约
      contains: class DecisionReadyEventData
    - path: apps/api/app/schemas/state.py
      provides: StateSnapshot diagnostics ledger 契约
      contains: class StateSnapshot
    - path: apps/api/app/services/sessions.py
      provides: turn decision 回放与 snapshot hydrate 的 diagnostics section/meta
      contains: _build_turn_decision_sections
  key_links:
    - from: apps/api/app/repositories/agent_turn_decisions.py
      to: apps/api/app/services/sessions.py
      via: state_patch_json diagnostics snapshot
      pattern: diagnostics|diagnostic_summary
    - from: apps/api/app/services/message_state.py
      to: apps/api/app/schemas/message.py
      via: DecisionReadyEventData
      pattern: ledger_summary|diagnostics
    - from: apps/api/app/services/sessions.py
      to: apps/api/app/schemas/state.py
      via: StateSnapshot
      pattern: diagnostics|diagnostic_summary
---

<objective>
把 Phase 2 的 diagnostics 契约贯通到持久化、SSE 和 session snapshot，使“本轮诊断快照”和“当前 open ledger”遵循与 Phase 1 guidance 相同的双层真源模式。

Purpose: 只有历史 turn decision 与当前 snapshot 都保存 diagnostics，刷新恢复、SSE 增量和后续回放分析才不会漂移。
Output: 统一的后端序列化契约、open ledger 聚合逻辑、session hydrate / message stream 回归测试。
</objective>

<execution_context>
@/Users/zhangyanhua/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhangyanhua/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-zhen-duan-shen-wa-yu-wen-ti-tai-zhang/02-RESEARCH.md
@.planning/phases/02-zhen-duan-shen-wa-yu-wen-ti-tai-zhang/02-VALIDATION.md
@.planning/phases/01-yin-dao-jie-zou-yu-xuan-xiang-cheng-qing/01-02-SUMMARY.md
@apps/api/app/repositories/agent_turn_decisions.py
@apps/api/app/schemas/message.py
@apps/api/app/schemas/state.py
@apps/api/app/services/message_state.py
@apps/api/app/services/sessions.py

<interfaces>
From apps/api/app/schemas/message.py:
```python
class DecisionReadyEventData(BaseModel):
    session_id: str
    user_message_id: str
    phase: str
    conversation_strategy: str
    next_move: str
```

From apps/api/app/schemas/state.py:
```python
class StateSnapshot(BaseModel):
    working_hypotheses: list[dict[str, Any]]
    pm_risk_flags: list[str]
    open_questions: list[str]
```

From apps/api/app/services/sessions.py:
```python
def _build_turn_decision_sections(decision: AgentTurnDecision) -> list[AgentTurnDecisionSectionResponse]:
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 持久化每轮 diagnostics 快照并通过 decision.ready 透出最新诊断</name>
  <read_first>
    .planning/phases/02-zhen-duan-shen-wa-yu-wen-ti-tai-zhang/02-RESEARCH.md
    .planning/phases/01-yin-dao-jie-zou-yu-xuan-xiang-cheng-qing/01-02-SUMMARY.md
    apps/api/app/repositories/agent_turn_decisions.py
    apps/api/app/schemas/message.py
    apps/api/app/services/message_state.py
    apps/api/tests/test_messages_stream.py
  </read_first>
  <files>apps/api/app/repositories/agent_turn_decisions.py, apps/api/app/schemas/message.py, apps/api/app/services/message_state.py, apps/api/tests/test_messages_stream.py</files>
  <behavior>
    - Test 1: `decision.ready` 事件新增 `diagnostics`、`diagnostic_summary`、`ledger_summary`，并带出本轮 open item 列表
    - Test 2: `agent_turn_decisions.state_patch_json` 保留每轮 diagnostics 快照，不依赖新增数据库列
    - Test 3: `decision.ready` 的 diagnostics 顺序稳定，按 `bucket` 统计的 summary 与 item 内容一致
  </behavior>
  <action>沿用 Phase 1 的“TurnDecision 是真源，SSE 只做序列化”模式，但本 phase 选择把 diagnostics 快照存进现有 JSON 字段，不新增独立数据库列。具体要求：1) 在 `agent_turn_decisions.py` 的 `_build_turn_decision_state_patch()` 中强制写入 `diagnostics` 与 `diagnostic_summary`，即使为空也要保留空数组/零计数；2) 在 `message_state.py` 新增 ledger 聚合 helper，把当前 `state["diagnostics"]` 与本轮 `TurnDecision.diagnostics` 合并成 open ledger，规则是按 stable id 去重、同 id 新项覆盖旧项、`status != open` 的项目不进入 ledger；3) `build_guidance_payload()` 不要承载 diagnostics，改为新增独立 `build_diagnostics_payload()` 或等价 helper，由消息流在构造 `DecisionReadyEventData` 时显式输出 `diagnostics`、`diagnostic_summary`、`ledger_summary`；4) `DecisionReadyEventData` 中本轮 items 与 ledger summary 必须是结构化字段，不允许把 diagnostics 混进 `recommendation` 或 `suggestions`；5) 为 `test_messages_stream.py` 增加断言，验证 `message.accepted -> ... -> decision.ready` 的顺序不变，同时 `decision.ready` 中 diagnostics 字段齐全、内容与 state patch 一致。</action>
  <acceptance_criteria>
    - `rg -n "diagnostics|diagnostic_summary|ledger_summary" apps/api/app/repositories/agent_turn_decisions.py apps/api/app/schemas/message.py apps/api/app/services/message_state.py` 返回命中
    - `rg -n "decision.ready|diagnostics|ledger_summary" apps/api/tests/test_messages_stream.py` 返回命中
    - `PYTHONPATH=apps/api uv run pytest apps/api/tests/test_messages_stream.py -q -k "diagnostic"` 通过
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=apps/api uv run pytest apps/api/tests/test_messages_stream.py -q -k "diagnostic"</automated>
  </verify>
  <done>每轮 turn decision 的 diagnostics 能稳定落入持久化 JSON，并通过 `decision.ready` 对外暴露结构化载荷。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 聚合 open diagnostics ledger 并让 session snapshot / decision sections 恢复一致</name>
  <read_first>
    .planning/phases/02-zhen-duan-shen-wa-yu-wen-ti-tai-zhang/02-RESEARCH.md
    apps/api/app/schemas/state.py
    apps/api/app/services/message_state.py
    apps/api/app/services/sessions.py
    apps/api/tests/test_sessions.py
  </read_first>
  <files>apps/api/app/schemas/state.py, apps/api/app/services/message_state.py, apps/api/app/services/sessions.py, apps/api/tests/test_sessions.py</files>
  <behavior>
    - Test 1: `StateSnapshot` 包含 open `diagnostics` 与 `diagnostic_summary`，刷新后能恢复 unknown/risk/to_validate 三组数据
    - Test 2: `turn_decisions[].decision_sections` 的 judgement / next_step meta 中能读到本轮 diagnostics summary 和 suggested next step
    - Test 3: 同一 session 多轮消息后，snapshot ledger 只保留 open 项，resolved 或 superseded 项不会反复出现
  </behavior>
  <action>把“历史事实”和“当前工作台状态”分层落实到 session API。具体要求：1) 在 `StateSnapshot` 增加 `diagnostics: list[dict[str, Any]]` 与 `diagnostic_summary: dict[str, Any]` 字段，其中 `diagnostics` 只存 open ledger，不存已解决项；2) 在 `message_state.py` 的 merge 流程里统一生成 `diagnostic_summary`，至少统计 `open_count`、`unknown_count`、`risk_count`、`to_validate_count`；3) 在 `sessions.py` 读取 `turn_decisions` 时，从 `state_patch_json["diagnostics"]` 和 `state_patch_json["diagnostic_summary"]` 回填 `decision_sections.meta`，让前端能同时获得“本轮诊断摘要”和“当前 ledger 汇总”；4) session hydrate 响应中的 `state` 必须直接带出 open ledger，禁止前端根据历史 turn decisions 自己重算；5) 不要把 diagnostics 注入右侧 PRD panel 相关 schema，本任务只允许动 `sessions.py`、`state.py` 和与会话列直接相关的 sections meta。</action>
  <acceptance_criteria>
    - `rg -n "diagnostics|diagnostic_summary|open_count|unknown_count|risk_count|to_validate_count" apps/api/app/schemas/state.py apps/api/app/services/message_state.py apps/api/app/services/sessions.py` 返回命中
    - `rg -n "diagnostics|diagnostic_summary|next_step|judgement" apps/api/tests/test_sessions.py` 返回命中
    - `PYTHONPATH=apps/api uv run pytest apps/api/tests/test_sessions.py -q -k "diagnostic"` 通过
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=apps/api uv run pytest apps/api/tests/test_sessions.py -q -k "diagnostic"</automated>
  </verify>
  <done>刷新后的 session snapshot 可以直接恢复 open diagnostics ledger，前端无需重算历史诊断项。</done>
</task>

</tasks>

<verification>
后端验证必须同时覆盖：
- `decision.ready` 暴露本轮 diagnostics 与 ledger summary
- `StateSnapshot` 暴露 open ledger 且刷新恢复一致
- `turn_decisions` 回放与 snapshot hydrate 使用同一套 diagnostics 数据源
</verification>

<success_criteria>
Phase 2 的双层真源建立完成后，后端能同时保留“每轮识别了什么诊断项”和“当前还开放着哪些未知项 / 风险 / 待验证项”，SSE 与 session hydrate 不再漂移。
</success_criteria>

<output>
After completion, create `.planning/phases/02-zhen-duan-shen-wa-yu-wen-ti-tai-zhang/02-02-SUMMARY.md`
</output>
