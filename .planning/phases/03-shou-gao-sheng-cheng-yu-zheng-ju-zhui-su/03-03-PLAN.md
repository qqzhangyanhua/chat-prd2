---
phase: 03-shou-gao-sheng-cheng-yu-zheng-ju-zhui-su
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - apps/web/src/lib/types.ts
  - apps/web/src/store/workspace-store.ts
  - apps/web/src/components/workspace/draft-evidence-drawer.tsx
  - apps/web/src/components/workspace/first-draft-card.tsx
  - apps/web/src/components/workspace/workspace-session-shell.tsx
  - apps/web/src/test/prd-panel.test.tsx
  - apps/web/src/test/workspace-session-shell.test.tsx
  - apps/web/src/test/workspace-store.test.ts
autonomous: true
requirements:
  - INTK-01
  - INTK-02
  - INTK-03
must_haves:
  truths:
    - 用户在会话列里能看到结构化首稿，而不是只能看右侧 PRD preview 或 assistant 文本
    - 每条首稿内容都清楚展示“已确认 / 推断 / 待验证”，不会把未确认内容伪装成既定事实
    - 点击首稿条目即可查看来源证据，不需要从历史聊天记录手动比对
  artifacts:
    - path: apps/web/src/lib/types.ts
      provides: first-draft / evidence 前端共享类型与 `draft.updated` 事件定义
      contains: interface FirstDraftSection
    - path: apps/web/src/store/workspace-store.ts
      provides: first-draft hydrate / SSE merge / stale snapshot 保护
      contains: applyEvent
    - path: apps/web/src/components/workspace/first-draft-card.tsx
      provides: 会话列最小首稿视图
      min_lines: 80
  key_links:
    - from: apps/web/src/lib/types.ts
      to: apps/web/src/store/workspace-store.ts
      via: `draft.updated` / snapshot types
      pattern: draft.updated|assertionState|evidenceRefIds
    - from: apps/web/src/store/workspace-store.ts
      to: apps/web/src/components/workspace/first-draft-card.tsx
      via: firstDraft view model
      pattern: sections|evidenceRegistry|latestUpdates
    - from: apps/web/src/components/workspace/workspace-session-shell.tsx
      to: apps/web/src/test/prd-panel.test.tsx
      via: 保持 conversation column 与 PRD column 边界
      pattern: PrdPanel
---

<objective>
把 Phase 3 的结构化首稿与证据追溯以最小 UI 落在会话列：新增首稿卡片和证据抽屉，但不改动右侧 `PrdPanel` 的 runtime preview 角色。

Purpose: 满足 INTK-01/02/03 的用户可见层，并守住与 Phase 2 diagnostics ledger、Phase 4 PRD panel 的职责边界。
Output: 前端 first-draft types、store 派生、会话列首稿卡片与 evidence drawer、`PrdPanel` 不混层回归测试。
</objective>

<execution_context>
@/Users/zhangyanhua/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhangyanhua/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-shou-gao-sheng-cheng-yu-zheng-ju-zhui-su/03-RESEARCH.md
@apps/web/src/lib/types.ts
@apps/web/src/store/workspace-store.ts
@apps/web/src/components/workspace/workspace-session-shell.tsx
@apps/web/src/components/workspace/prd-panel.tsx
@apps/web/src/test/workspace-store.test.ts
@apps/web/src/test/workspace-session-shell.test.tsx
@apps/web/src/test/prd-panel.test.tsx

<interfaces>
From apps/web/src/lib/types.ts:
```typescript
export type WorkspaceEvent =
  | { type: "decision.ready"; data: DecisionReadyData }
  | { type: "prd.updated"; data: { sections: Record<string, PrdSection>; meta?: PrdMeta } };
```

From apps/web/src/store/workspace-store.ts:
```typescript
interface WorkspaceState {
  prd: PrdState;
  diagnosticLedger: DecisionDiagnosticItem[];
  hydrateSession: (snapshot: SessionSnapshotResponse) => void;
  applyEvent: (event: WorkspaceEvent) => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展前端 types 与 store，让 first-draft 成为独立 view model 而不是复用 prd panel state</name>
  <read_first>
    .planning/phases/03-shou-gao-sheng-cheng-yu-zheng-ju-zhui-su/03-RESEARCH.md
    apps/web/src/lib/types.ts
    apps/web/src/store/workspace-store.ts
    apps/web/src/test/workspace-store.test.ts
  </read_first>
  <files>apps/web/src/lib/types.ts, apps/web/src/store/workspace-store.ts, apps/web/src/test/workspace-store.test.ts</files>
  <action>在前端引入 Phase 3 独立 state，但不要复用右侧 `prd` store。具体要求：1) 在 `types.ts` 新增 `AssertionState`、`FirstDraftEntry`、`FirstDraftSection`、`EvidenceItem`、`FirstDraftState`，并为 `StateSnapshotResponse`、`DecisionReadyData` / 新 `DraftUpdatedData` 定义对应字段；2) 扩展 `WorkspaceEvent` 支持 `draft.updated`，但保留现有 `prd.updated` 原样不变；3) 在 `workspace-store.ts` 中新增独立 `firstDraft` state，至少包含 `sections`、`evidenceRegistry`、`latestUpdates`，并让 `hydrateSession()` 读取 snapshot 中的 enriched `prd_draft/evidence`，`applyEvent()` 读取 `draft.updated`；4) first-draft 与现有 diagnostics/prd state 必须彼此独立，不允许把首稿条目塞进 `state.prd` 或 `diagnosticLedger`；5) 继续沿用现有 stale snapshot 保护，如果 incoming snapshot 的 draft version 更旧，不要覆盖当前更完整的 first-draft state；6) `workspace-store.test.ts` 增加 hydrate、`draft.updated` merge、stale snapshot 保护和“不污染 prd panel state”的断言。</action>
  <acceptance_criteria>
    - `rg -n "FirstDraft|draft.updated|assertionState|evidenceRegistry|latestUpdates" apps/web/src/lib/types.ts apps/web/src/store/workspace-store.ts` 返回命中
    - `rg -n "draft.updated|firstDraft|stale snapshot|does not mutate prd" apps/web/src/test/workspace-store.test.ts` 返回命中
    - `cd apps/web && pnpm test -- src/test/workspace-store.test.ts -t "draft|evidence"` 通过
  </acceptance_criteria>
  <verify>
    <automated>cd apps/web && pnpm test -- src/test/workspace-store.test.ts -t "draft|evidence"</automated>
  </verify>
  <done>前端 store 能独立持有结构化首稿与 evidence registry，同时不破坏现有 diagnostics ledger 和 PRD preview state。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 在会话列渲染结构化首稿与证据抽屉，并锁定不侵入 PrdPanel</name>
  <read_first>
    .planning/phases/03-shou-gao-sheng-cheng-yu-zheng-ju-zhui-su/03-RESEARCH.md
    apps/web/src/components/workspace/workspace-session-shell.tsx
    apps/web/src/components/workspace/prd-panel.tsx
    apps/web/src/test/workspace-session-shell.test.tsx
    apps/web/src/test/prd-panel.test.tsx
  </read_first>
  <files>apps/web/src/components/workspace/first-draft-card.tsx, apps/web/src/components/workspace/draft-evidence-drawer.tsx, apps/web/src/components/workspace/workspace-session-shell.tsx, apps/web/src/test/workspace-session-shell.test.tsx, apps/web/src/test/prd-panel.test.tsx</files>
  <action>把首稿 UI 放在会话列，不提前重做右侧 panel。具体要求：1) 新建 `first-draft-card.tsx`，按 section 渲染 `target_user`、`problem`、`solution`、`mvp_scope`、`success_metrics` 等首稿内容，并对每条 entry 显示明确的中文状态标签：`已确认`、`推断`、`待验证`；2) 新建 `draft-evidence-drawer.tsx`，当用户点击某条 entry 的“查看来源”时显示对应 evidence registry 内容，最小展示 `kind`、`excerpt`、关联 section 与时间/来源 id；3) 在 `workspace-session-shell.tsx` 的 conversation column 中挂载 `FirstDraftCard`，位置与 diagnostics ledger 并列但不混在同一卡片中，明确不向 `PrdPanel` 传递任何 first-draft props；4) `PrdPanel` 组件本身不改，只在 `prd-panel.test.tsx` 增加回归：即使收到了 `draft.updated` 或存在 first-draft state，右侧 panel 仍只展示 `prd.updated` 派生内容；5) `workspace-session-shell.test.tsx` 需覆盖 snapshot hydrate 后首稿展示、entry 状态徽章、evidence drawer 打开、以及会话列/PRD 列边界。</action>
  <acceptance_criteria>
    - `test -f apps/web/src/components/workspace/first-draft-card.tsx` 成功
    - `test -f apps/web/src/components/workspace/draft-evidence-drawer.tsx` 成功
    - `rg -n "已确认|推断|待验证|查看来源" apps/web/src/components/workspace/first-draft-card.tsx apps/web/src/components/workspace/draft-evidence-drawer.tsx` 返回命中
    - `rg -n "draft.updated|PrdPanel|first-draft|evidence drawer" apps/web/src/test/workspace-session-shell.test.tsx apps/web/src/test/prd-panel.test.tsx` 返回命中
    - `cd apps/web && pnpm test -- src/test/workspace-session-shell.test.tsx src/test/prd-panel.test.tsx -t "draft|evidence|panel"` 通过
  </acceptance_criteria>
  <verify>
    <automated>cd apps/web && pnpm test -- src/test/workspace-session-shell.test.tsx src/test/prd-panel.test.tsx -t "draft|evidence|panel"</automated>
  </verify>
  <done>用户在会话列能看到结构化首稿和逐条来源追溯，而右侧 `PrdPanel` 仍然只承担 runtime preview，不和 Phase 3 首稿 UI 混层。</done>
</task>

</tasks>

<verification>
前端验证必须同时覆盖：
- session snapshot 与 `draft.updated` 都能恢复/更新 first-draft state
- 会话列可见条目级 assertion state 与 evidence drawer
- `PrdPanel` 不消费 first-draft state，Phase 4 边界未被提前打穿
</verification>

<success_criteria>
Phase 3 的用户可见层完成后，用户可以在会话列直接看到结构化首稿、区分已确认/推断/待验证，并逐条查看来源证据，而无需切换到右侧 PRD panel 或翻聊天记录手动比对。
</success_criteria>

<output>
After completion, create `.planning/phases/03-shou-gao-sheng-cheng-yu-zheng-ju-zhui-su/03-03-SUMMARY.md`
</output>
