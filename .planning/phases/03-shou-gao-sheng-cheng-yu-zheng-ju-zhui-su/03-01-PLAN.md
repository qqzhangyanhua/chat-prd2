---
phase: 03-shou-gao-sheng-cheng-yu-zheng-ju-zhui-su
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/app/agent/types.py
  - apps/api/app/agent/pm_mentor.py
  - apps/api/app/schemas/state.py
  - apps/api/tests/test_agent_types_contract.py
  - apps/api/tests/test_message_state.py
  - apps/api/tests/test_pm_mentor.py
autonomous: true
requirements:
  - INTK-01
  - INTK-02
  - INTK-03
must_haves:
  truths:
    - 后端能生成覆盖目标用户、核心问题、方案方向、范围边界与成功标准的结构化首稿条目，而不是只返回 section 级字符串
    - 每条首稿内容都明确标明 `confirmed / inferred / to_validate`，不会再把“待验证”错误压扁成 `missing`
    - 每条首稿内容都直接引用 evidence id，前端和回放层无需从聊天文本倒推来源
  artifacts:
    - path: apps/api/app/agent/types.py
      provides: DraftEntry / DraftSection / EvidenceItem / AssertionState / DraftUpdateSummary 契约
      contains: class TurnDecision
    - path: apps/api/app/schemas/state.py
      provides: enriched prd_draft 与 evidence registry persisted schema
      contains: class StateSnapshot
    - path: apps/api/app/agent/pm_mentor.py
      provides: 首稿 sections / assertion_state / evidence candidates 生成规则
      contains: run_pm_mentor
  key_links:
    - from: apps/api/app/agent/pm_mentor.py
      to: apps/api/app/agent/types.py
      via: TurnDecision.state_patch["prd_draft"] / ["evidence"]
      pattern: assertion_state|evidence_ref_ids
    - from: apps/api/app/agent/types.py
      to: apps/api/app/schemas/state.py
      via: persisted prd_draft / evidence contract
      pattern: entries|completeness|evidence
    - from: apps/api/app/agent/pm_mentor.py
      to: apps/api/tests/test_pm_mentor.py
      via: 结构化首稿生成断言
      pattern: target_user|problem|solution|mvp_scope|success_metrics
---

<objective>
为 Phase 3 建立首稿与证据追溯的后端一等契约：在 `state.prd_draft` 与 `state.evidence` 上定义 entry 级内容、断言状态与来源引用，并让 `pm_mentor` 开始生成这些结构。

Purpose: 先固定 persisted truth 的 shape，后续的 SSE、snapshot、前端首稿卡片和导出兼容层才能围绕同一份 contract 工作。
Output: enriched `prd_draft` / `evidence` schema、entry 级 `assertion_state` 规则、带 evidence refs 的 agent 输出测试。
</objective>

<execution_context>
@/Users/zhangyanhua/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhangyanhua/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-shou-gao-sheng-cheng-yu-zheng-ju-zhui-su/03-RESEARCH.md
@apps/api/app/agent/types.py
@apps/api/app/agent/pm_mentor.py
@apps/api/app/schemas/state.py
@apps/api/app/services/message_state.py

<interfaces>
From apps/api/app/schemas/state.py:
```python
class StateSnapshot(BaseModel):
    prd_draft: dict[str, Any] | None = None
    evidence: list[dict[str, Any]] = Field(default_factory=list)
```

From apps/api/app/agent/types.py:
```python
class TurnDecision:
    prd_patch: dict[str, Any]
    state_patch: dict[str, Any]
    diagnostics: list[DiagnosticItem]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 定义 first-draft 与 evidence registry 契约，并把 completeness 与 assertion_state 分层</name>
  <read_first>
    .planning/phases/03-shou-gao-sheng-cheng-yu-zheng-ju-zhui-su/03-RESEARCH.md
    apps/api/app/agent/types.py
    apps/api/app/schemas/state.py
    apps/api/tests/test_agent_types_contract.py
    apps/api/tests/test_message_state.py
  </read_first>
  <files>apps/api/app/agent/types.py, apps/api/app/schemas/state.py, apps/api/tests/test_agent_types_contract.py, apps/api/tests/test_message_state.py</files>
  <action>把 Phase 3 的 persisted truth 先建起来。具体要求：1) 在 `apps/api/app/agent/types.py` 新增 `AssertionState = confirmed|inferred|to_validate`、`DraftCompleteness = complete|partial|missing`、`EvidenceKind = user_message|assistant_decision|system_inference|diagnostic`、`DraftEntry`、`DraftSection`、`EvidenceItem`、`DraftUpdateSummary` 类型；2) `DraftEntry` 至少包含 `id`、`text`、`assertion_state`、`evidence_ref_ids`，`DraftSection` 至少包含 `title`、`entries`、`completeness`，禁止继续把 Phase 3 语义压回单个 `content/status`；3) 在 `StateSnapshot` 中把 `prd_draft` 的最小 persisted shape 明确成 `version/status/sections`，并让 `evidence` 变成结构化 registry，而不是自由字典列表；4) 显式约束 `assertion_state` 与 `completeness` 是两层语义，不能把 `to_validate` 投影成 `missing`；5) 保留与现有 finalize/export 兼容所需的过渡空间，例如允许 section 中同时保存 `summary` 或 projection metadata，但不要在本任务里改 `exports.py` / `finalize_flow.py`；6) 测试覆盖 contract 序列化、非法 assertion state 拒绝、evidence refs 为 entry 级字段而非 section 级字段。</action>
  <acceptance_criteria>
    - `rg -n "AssertionState|DraftEntry|DraftSection|EvidenceItem|DraftUpdateSummary" apps/api/app/agent/types.py` 返回命中
    - `rg -n "assertion_state|evidence_ref_ids|completeness|evidence" apps/api/app/schemas/state.py apps/api/tests/test_agent_types_contract.py apps/api/tests/test_message_state.py` 返回命中
    - `PYTHONPATH=apps/api uv run pytest apps/api/tests/test_agent_types_contract.py apps/api/tests/test_message_state.py -q -k "draft or evidence or assertion"` 通过
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=apps/api uv run pytest apps/api/tests/test_agent_types_contract.py apps/api/tests/test_message_state.py -q -k "draft or evidence or assertion"</automated>
  </verify>
  <done>首稿与证据追溯的后端 contract 被固定下来，后续层可以直接围绕 `entries + assertion_state + evidence_ref_ids` 编排。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 让 pm_mentor 输出结构化首稿条目与 evidence candidates，但不混入 diagnostics ledger</name>
  <read_first>
    .planning/phases/03-shou-gao-sheng-cheng-yu-zheng-ju-zhui-su/03-RESEARCH.md
    apps/api/app/agent/pm_mentor.py
    apps/api/app/agent/types.py
    apps/api/tests/test_pm_mentor.py
  </read_first>
  <files>apps/api/app/agent/pm_mentor.py, apps/api/app/agent/types.py, apps/api/tests/test_pm_mentor.py</files>
  <action>把首稿生成逻辑限制在 agent 层，并与 Phase 2 diagnostics 分层。具体要求：1) 扩展 `pm_mentor.py` 的输出归一化逻辑，使其在每轮返回 `prd_draft` section entry updates 与 `evidence` candidates，而不是只返回 `prd_updates: {section: {content,status}}`；2) 至少支持 `target_user`、`problem`、`solution`、`mvp_scope`、`success_metrics` 五类结构化 sections，其中“范围边界”通过 `mvp_scope` 与后续 `constraints/out_of_scope` 入口体现，不要求在本任务里重写 Phase 4 panel；3) `assertion_state` 赋值规则必须可测试：用户明确确认 -> `confirmed`，系统基于上下文合理补全但用户未明确确认 -> `inferred`，内容已形成但仍待后续验证 -> `to_validate`；4) 为每条 entry 绑定至少一个 `evidence_ref_id`，最小可交付优先支持 `user_message` 与 `system_inference` 两类 evidence，不允许把 diagnostics item 直接当正文 evidence；5) `diagnostics` 仍然只留在 diagnostics ledger 路径中，禁止把 `unknown/risk/to_validate` bucket 直接渲染成首稿 entry 文本；6) 对旧 `prd_updates` 保留兼容投影数据，供 03-02 的兼容层使用，但不要让它继续作为 Phase 3 真源。</action>
  <acceptance_criteria>
    - `rg -n "assertion_state|evidence_ref_ids|user_message|system_inference|prd_draft" apps/api/app/agent/pm_mentor.py apps/api/tests/test_pm_mentor.py` 返回命中
    - `rg -n "target_user|problem|solution|mvp_scope|success_metrics" apps/api/tests/test_pm_mentor.py` 返回命中
    - `PYTHONPATH=apps/api uv run pytest apps/api/tests/test_pm_mentor.py -q -k "draft or evidence"` 通过
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=apps/api uv run pytest apps/api/tests/test_pm_mentor.py -q -k "draft or evidence"</automated>
  </verify>
  <done>`pm_mentor` 能生成 entry 级首稿与证据候选，且该输出与 diagnostics ledger 明确分层。</done>
</task>

</tasks>

<verification>
后端验证必须同时覆盖：
- `prd_draft` 使用 entry 级 contract 而非旧单字符串结构
- `assertion_state` 与 `completeness` 被分层建模
- evidence 通过 registry + ref ids 连接，不靠前端倒推聊天文本
</verification>

<success_criteria>
Phase 3 的首稿 contract 完成后，系统已经能在 persisted state 中稳定表达“写了什么内容、这句话是已确认还是推断/待验证、它源自哪条证据”，而这三者彼此不再耦合到 diagnostics ledger 或 PRD panel preview。
</success_criteria>

<output>
After completion, create `.planning/phases/03-shou-gao-sheng-cheng-yu-zheng-ju-zhui-su/03-01-SUMMARY.md`
</output>
