---
phase: 03-shou-gao-sheng-cheng-yu-zheng-ju-zhui-su
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - apps/api/app/agent/finalize_flow.py
  - apps/api/app/schemas/message.py
  - apps/api/app/services/exports.py
  - apps/api/app/services/message_state.py
  - apps/api/app/services/messages.py
  - apps/api/app/services/sessions.py
  - apps/api/tests/test_finalize_session.py
  - apps/api/tests/test_messages_service.py
  - apps/api/tests/test_messages_stream.py
  - apps/api/tests/test_sessions.py
autonomous: true
requirements:
  - INTK-01
  - INTK-02
  - INTK-03
must_haves:
  truths:
    - 首稿 enriched state 会随着消息流持续持久化，并通过 SSE 与 session snapshot 恢复
    - 用户刷新页面后仍能看到同一份结构化首稿与 evidence registry，不会因为仅靠前端缓存而丢失
    - 导出与 finalize 流程不会因为 `prd_draft` 结构升级而中断
  artifacts:
    - path: apps/api/app/schemas/message.py
      provides: `draft.updated` 事件契约
      contains: class DraftUpdatedEventData
    - path: apps/api/app/services/message_state.py
      provides: enriched prd_draft / evidence merge 与兼容 projection 逻辑
      contains: merge_state_patch_with_decision
    - path: apps/api/app/services/sessions.py
      provides: snapshot hydrate 与 turn decision draft/evidence meta 回放
      contains: _build_turn_decision_sections
  key_links:
    - from: apps/api/app/services/messages.py
      to: apps/api/app/schemas/message.py
      via: draft.updated SSE event
      pattern: draft.updated
    - from: apps/api/app/services/message_state.py
      to: apps/api/app/services/exports.py
      via: 兼容 projection for legacy content/status readers
      pattern: normalize|projection
    - from: apps/api/app/services/sessions.py
      to: apps/api/tests/test_sessions.py
      via: snapshot draft/evidence hydrate
      pattern: prd_draft|evidence
---

<objective>
把 Phase 3 的首稿 contract 贯通到持久化、SSE、session snapshot 和兼容层：新增明确的 `draft.updated` 事件，同时保证 `exports.py` 与 `finalize_flow.py` 不被 enriched `prd_draft` 结构击穿。

Purpose: contract 定义完之后，必须把“当前首稿状态、每轮更新、刷新恢复、旧流程兼容”全部接到现有消息管线里，否则 Phase 3 只能停留在内存结构。
Output: `draft.updated` 事件、snapshot/turn decision 回放字段、enriched draft 与 export/finalize 的兼容投影、后端集成测试。
</objective>

<execution_context>
@/Users/zhangyanhua/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhangyanhua/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-shou-gao-sheng-cheng-yu-zheng-ju-zhui-su/03-RESEARCH.md
@apps/api/app/services/messages.py
@apps/api/app/services/message_state.py
@apps/api/app/services/sessions.py
@apps/api/app/services/exports.py
@apps/api/app/agent/finalize_flow.py

<interfaces>
From apps/api/app/services/messages.py:
```python
def _build_decision_ready_event(guidance: dict[str, object]) -> MessageStreamEvent:
    return MessageStreamEvent(type="decision.ready", data=DecisionReadyEventData(**guidance).model_dump())
```

From apps/api/app/services/exports.py:
```python
def build_export_sections(state: dict, snapshot: dict) -> tuple[dict, bool]:
    prd_draft = state.get("prd_draft")
```

From apps/api/app/agent/finalize_flow.py:
```python
def normalize_prd_draft_sections(prd_draft: dict) -> dict[str, dict[str, str]]:
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 贯通 enriched prd_draft / evidence 持久化，并新增 draft.updated 事件与 snapshot 回放</name>
  <read_first>
    .planning/phases/03-shou-gao-sheng-cheng-yu-zheng-ju-zhui-su/03-RESEARCH.md
    apps/api/app/services/message_state.py
    apps/api/app/services/messages.py
    apps/api/app/services/sessions.py
    apps/api/app/schemas/message.py
    apps/api/tests/test_messages_service.py
    apps/api/tests/test_messages_stream.py
    apps/api/tests/test_sessions.py
  </read_first>
  <files>apps/api/app/schemas/message.py, apps/api/app/services/message_state.py, apps/api/app/services/messages.py, apps/api/app/services/sessions.py, apps/api/tests/test_messages_service.py, apps/api/tests/test_messages_stream.py, apps/api/tests/test_sessions.py</files>
  <action>把首稿状态接入现有消息管线，但不要污染 Phase 4 `prd.updated` 语义。具体要求：1) 在 `apps/api/app/schemas/message.py` 新增 `DraftUpdatedEventData`，其 payload 至少包含 `sections`、`evidence_registry`、`draft_summary`、`sections_changed` 与 `entry_ids`；2) 在 `message_state.py` 增加 enriched `prd_draft` / `evidence` merge helper，规则是：同 id entry 覆盖旧值、registry 去重、state 中永远保存最新完整 draft truth；3) 在 `messages.py` 的 SSE 序列中新增 `draft.updated`，位置放在 `decision.ready` 之后、`assistant.version.started` 之前或与首稿写入完成紧邻，但必须写清楚并在测试中锁定顺序；4) 保持现有 `prd.updated` 继续只服务右侧 `PrdPanel` preview，禁止把 evidence-heavy draft payload 塞进 `prd.updated`；5) `sessions.py` 在 session snapshot 中返回 enriched `state.prd_draft` 与 `state.evidence`，同时在 `turn_decisions.decision_sections.meta` 回填本轮 `draft_updates` / `evidence_ref_ids` 摘要，便于后续 UI 展示“本轮新增了什么”；6) `test_messages_service.py`、`test_messages_stream.py`、`test_sessions.py` 需新增断言，覆盖 INTK-01/02/03 的集成路径。</action>
  <acceptance_criteria>
    - `rg -n "draft.updated|DraftUpdatedEventData|evidence_registry|sections_changed|entry_ids" apps/api/app/schemas/message.py apps/api/app/services/messages.py apps/api/tests/test_messages_stream.py` 返回命中
    - `rg -n "prd_draft|evidence|draft_updates|evidence_ref_ids" apps/api/app/services/message_state.py apps/api/app/services/sessions.py apps/api/tests/test_messages_service.py apps/api/tests/test_sessions.py` 返回命中
    - `PYTHONPATH=apps/api uv run pytest apps/api/tests/test_messages_service.py apps/api/tests/test_messages_stream.py apps/api/tests/test_sessions.py -q -k "draft or evidence"` 通过
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=apps/api uv run pytest apps/api/tests/test_messages_service.py apps/api/tests/test_messages_stream.py apps/api/tests/test_sessions.py -q -k "draft or evidence"</automated>
  </verify>
  <done>首稿与证据追溯已进入 persisted state、SSE 与 snapshot 主链路，刷新和回放都能拿到同一份结构化首稿真源。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 为 finalize / export 提供 enriched draft 兼容投影，并锁定不破坏旧 PRD runtime</name>
  <read_first>
    .planning/phases/03-shou-gao-sheng-cheng-yu-zheng-ju-zhui-su/03-RESEARCH.md
    apps/api/app/agent/finalize_flow.py
    apps/api/app/services/exports.py
    apps/api/app/services/prd_runtime.py
    apps/api/tests/test_finalize_session.py
    apps/api/tests/test_messages_stream.py
  </read_first>
  <files>apps/api/app/agent/finalize_flow.py, apps/api/app/services/exports.py, apps/api/tests/test_finalize_session.py, apps/api/tests/test_messages_stream.py</files>
  <action>兼容层必须显式存在，避免 enriched `prd_draft` 一落地就打断导出和终稿整理。具体要求：1) 在 `finalize_flow.py` 中把 `normalize_prd_draft_sections()` 升级为先读取 enriched `entries`，再投影回当前 finalize 所需的 `{title, content, status}` section 结构；投影规则必须明确：多个 entries 组合成 section content，`to_validate` 不等于 `missing`；2) 在 `exports.py` 中优先从 enriched `prd_draft.sections[].entries` 生成 Markdown 导出内容，并在内容中保留“待验证” section/条目的兼容展示，不允许因为 entry 模式而导出空文档；3) 不修改 `prd_runtime.py` 的 event 名称与核心语义，只在测试中锁定“Phase 3 不污染 `prd.updated`”这一点；4) `test_finalize_session.py` 要覆盖 enriched draft 仍可整理终稿，`test_messages_stream.py` 要断言 `prd.updated` 结构未被 `draft.updated` payload 混入。</action>
  <acceptance_criteria>
    - `rg -n "entries|assertion_state|to_validate|normalize_prd_draft_sections" apps/api/app/agent/finalize_flow.py apps/api/app/services/exports.py` 返回命中
    - `rg -n "draft.updated|prd.updated|finalize" apps/api/tests/test_finalize_session.py apps/api/tests/test_messages_stream.py` 返回命中
    - `PYTHONPATH=apps/api uv run pytest apps/api/tests/test_finalize_session.py apps/api/tests/test_messages_stream.py -q -k "draft or finalize or prd_updated"` 通过
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=apps/api uv run pytest apps/api/tests/test_finalize_session.py apps/api/tests/test_messages_stream.py -q -k "draft or finalize or prd_updated"</automated>
  </verify>
  <done>enriched 首稿结构与现有 finalize/export 流程兼容，且 Phase 4 的 `prd.updated` runtime contract 没有被提前混层。</done>
</task>

</tasks>

<verification>
后端验证必须同时覆盖：
- `draft.updated` 提供首稿与 evidence 结构化增量
- session snapshot 能恢复完整 enriched `prd_draft` / `evidence`
- finalize/export 与 `prd.updated` preview 语义保持兼容，不被 Phase 3 破坏
</verification>

<success_criteria>
Phase 3 的集成链路完成后，系统可以在不影响现有 PRD preview、导出和终稿整理的前提下，稳定持久化并回放带 assertion state 与 evidence refs 的结构化首稿。
</success_criteria>

<output>
After completion, create `.planning/phases/03-shou-gao-sheng-cheng-yu-zheng-ju-zhui-su/03-02-SUMMARY.md`
</output>
