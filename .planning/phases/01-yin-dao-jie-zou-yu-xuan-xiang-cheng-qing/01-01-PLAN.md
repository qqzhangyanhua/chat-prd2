---
phase: 01-yin-dao-jie-zou-yu-xuan-xiang-cheng-qing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/app/agent/types.py
  - apps/api/app/agent/pm_mentor.py
  - apps/api/app/agent/runtime.py
  - apps/api/app/schemas/message.py
  - apps/api/app/schemas/state.py
  - apps/api/app/services/message_state.py
  - apps/api/app/services/messages.py
  - apps/api/app/services/sessions.py
  - apps/api/tests/test_agent_runtime.py
  - apps/api/tests/test_messages_stream.py
  - apps/api/tests/test_pm_mentor.py
  - apps/api/tests/test_sessions.py
autonomous: true
requirements:
  - GUID-01
  - GUID-02
  - GUID-04
must_haves:
  truths:
    - 用户触发新一轮澄清后，后端能明确区分当前处于 explore、narrow、compare 或 confirm 节奏
    - 每条 decision.ready 与会话快照都能说明本轮在推进哪个维度、为何现在切换该节奏
    - 后端能给前端提供显式的下一步动作类型与模式切换候选，而不是只输出旧的 conversation_strategy
  artifacts:
    - path: apps/api/app/agent/types.py
      provides: TurnDecision 显式 guidance contract 字段
      min_lines: 80
    - path: apps/api/app/schemas/message.py
      provides: decision.ready 事件结构化字段定义
      contains: class DecisionReadyEventData
    - path: apps/api/app/services/message_state.py
      provides: guidance payload 与 state patch 归一化逻辑
      contains: build_guidance_payload
    - path: apps/api/app/services/sessions.py
      provides: session snapshot 中的 guidance meta 暴露
      contains: _build_turn_decision_sections
  key_links:
    - from: apps/api/app/agent/pm_mentor.py
      to: apps/api/app/services/message_state.py
      via: TurnDecision -> build_guidance_payload
      pattern: guidance_mode|guidance_step|focus_dimension
    - from: apps/api/app/services/message_state.py
      to: apps/api/app/schemas/message.py
      via: DecisionReadyEventData
      pattern: option_cards|freeform_affordance|available_mode_switches
    - from: apps/api/app/services/sessions.py
      to: apps/api/tests/test_sessions.py
      via: session snapshot guidance meta
      pattern: transition_reason|strategy_label
---

<objective>
为 Phase 1 建立后端 guidance contract 的真源：TurnDecision、decision.ready 事件、state snapshot 和 session snapshot 统一输出“先探索、再逐步收紧”的显式节奏与切换理由。

Purpose: 先把后端 contract 扩完整，前端才能只做渲染而不再猜测模式、推进维度和下一步动作。
Output: 可被前端直接消费的 guidance 字段、回填到 snapshot 的状态数据，以及覆盖 SSE/快照一致性的后端自动化测试。
</objective>

<execution_context>
@/Users/zhangyanhua/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhangyanhua/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-yin-dao-jie-zou-yu-xuan-xiang-cheng-qing/01-RESEARCH.md
@apps/api/app/agent/types.py
@apps/api/app/services/message_state.py
@apps/api/app/services/sessions.py
@apps/api/app/schemas/message.py
@apps/api/app/schemas/state.py

<interfaces>
From apps/api/app/agent/types.py:
```python
class TurnDecision:
    next_move: NextMove
    suggestions: list[Suggestion]
    recommendation: dict[str, Any] | None
    next_best_questions: list[str]
    conversation_strategy: ConversationStrategy = "clarify"
    phase_subfocus: str | None = None
```

From apps/api/app/services/message_state.py:
```python
def build_guidance_payload(
    decision: object,
    *,
    session_id: str | None = None,
    user_message_id: str | None = None,
    conversation_strategy: str | None = None,
    next_best_questions: list[str] | None = None,
    confirm_quick_replies: list[str] | None = None,
) -> dict[str, object]:
```

From apps/api/app/schemas/message.py:
```python
class DecisionReadyEventData(BaseModel):
    session_id: str
    user_message_id: str
    phase: str
    conversation_strategy: str
    next_move: str
    suggestions: list[dict[str, object]]
    recommendation: dict[str, object] | None = None
    next_best_questions: list[str]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展 TurnDecision 与 SSE guidance contract</name>
  <read_first>
    .planning/phases/01-yin-dao-jie-zou-yu-xuan-xiang-cheng-qing/01-RESEARCH.md
    apps/api/app/agent/types.py
    apps/api/app/agent/pm_mentor.py
    apps/api/app/agent/runtime.py
    apps/api/app/services/message_state.py
    apps/api/app/schemas/message.py
    apps/api/tests/test_pm_mentor.py
    apps/api/tests/test_agent_runtime.py
    apps/api/tests/test_messages_stream.py
  </read_first>
  <files>apps/api/app/agent/types.py, apps/api/app/agent/pm_mentor.py, apps/api/app/agent/runtime.py, apps/api/app/services/message_state.py, apps/api/app/schemas/message.py, apps/api/tests/test_pm_mentor.py, apps/api/tests/test_agent_runtime.py, apps/api/tests/test_messages_stream.py</files>
  <behavior>
    - Test 1: `decision.ready` payload 包含 `guidance_mode`、`guidance_step`、`focus_dimension`、`transition_reason`、`option_cards`、`freeform_affordance`、`available_mode_switches`
    - Test 2: `option_cards` 数量被限制在 2-4 个，并保留稳定 `id`
    - Test 3: `freeform_affordance` 默认包含“都不对，我补充”入口，且 `transition_reason` 不为空字符串
  </behavior>
  <action>按研究结论实现 Phase 1 基础 contract。具体要求：1) 在 `TurnDecision` 与相关 dataclass 中新增 `guidance_mode`（取值至少覆盖 `explore`、`narrow`、`compare`、`confirm`）、`guidance_step`（至少覆盖 `answer`、`choose`、`compare`、`confirm`、`freeform`）、`focus_dimension`、`transition_reason`、`option_cards`、`freeform_affordance`、`transition_trigger`、`can_switch_mode`、`available_mode_switches` 字段；2) 在 `pm_mentor.py` 与 `runtime.py` 中为默认 greet、clarify、converge、confirm 路径填充这些字段，保持“先探索、再逐步收紧” per GUID-01，并把本轮推进维度限定为 user/problem/solution/boundary/constraint/validation 六类之一 per GUID-02；3) 在 `build_guidance_payload` 和 `DecisionReadyEventData` 中输出上述字段，同时保留旧字段 `conversation_strategy`、`next_move`、`suggestions` 以兼容现有流式链路；4) `option_cards` 要从现有 suggestions/recommendation 派生，生成稳定 `id`，最多 4 个，最少 2 个（若现有建议不足则只保留真实数量，不要伪造占位项）；5) `freeform_affordance` 必须是结构化对象，至少含 `label: "都不对，我补充"`、`value: "freeform"`、`kind: "freeform"`。</action>
  <acceptance_criteria>
    - `rg -n "guidance_mode|guidance_step|focus_dimension|transition_reason|option_cards|freeform_affordance|available_mode_switches" apps/api/app/agent/types.py apps/api/app/services/message_state.py apps/api/app/schemas/message.py` 返回命中新字段
    - `rg -n "都不对，我补充" apps/api/app/agent/pm_mentor.py apps/api/app/agent/runtime.py apps/api/app/services/message_state.py` 返回至少 1 条结果
    - `pytest apps/api/tests/test_pm_mentor.py apps/api/tests/test_agent_runtime.py apps/api/tests/test_messages_stream.py -q -k "guidance or structured"` 通过
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && pytest tests/test_pm_mentor.py tests/test_agent_runtime.py tests/test_messages_stream.py -q -k "guidance or structured"</automated>
  </verify>
  <done>后端流式 guidance payload 不再只依赖 `conversation_strategy`/`suggestions`；新增字段可从 `decision.ready` 直接读到，并被测试覆盖。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 让 session snapshot 与 turn decision sections 暴露同一份 guidance 真相</name>
  <read_first>
    .planning/phases/01-yin-dao-jie-zou-yu-xuan-xiang-cheng-qing/01-RESEARCH.md
    apps/api/app/services/sessions.py
    apps/api/app/schemas/state.py
    apps/api/app/services/message_state.py
    apps/api/tests/test_sessions.py
    apps/api/tests/test_messages_stream.py
  </read_first>
  <files>apps/api/app/services/sessions.py, apps/api/app/schemas/state.py, apps/api/app/services/message_state.py, apps/api/tests/test_sessions.py, apps/api/tests/test_messages_stream.py</files>
  <behavior>
    - Test 1: `GET /api/sessions/{id}` 返回的 `turn_decisions[].decision_sections[].meta` 含 `guidance_mode`、`focus_dimension`、`transition_reason`
    - Test 2: snapshot state 持久化 guidance 节奏相关字段，刷新后无需重新推断
    - Test 3: 同一轮 SSE `decision.ready` 与 session snapshot 中的 option/freeform/mode switch 数据一致
  </behavior>
  <action>把 Task 1 的 contract 双写到 snapshot 和 session 响应中。具体要求：1) 在 `build_decision_state_patch`、`StateSnapshot` 及相关归并逻辑中持久化 `guidance_mode`、`guidance_step`、`focus_dimension`、`transition_reason`、`available_mode_switches`、`freeform_affordance`，以满足刷新恢复 per GUID-04；2) 在 `_build_turn_decision_sections` 生成的 `judgement` / `next_step` meta 内补齐这些字段，并继续输出 `strategy_label` 作为兼容展示文本；3) 保证 snapshot 与 SSE 共用 `build_guidance_payload` 的归一化结果，避免 session 接口与流式接口字段漂移；4) 为 `test_sessions.py` 增加断言，明确 compare/confirm 模式下的 `guidance_mode`、`focus_dimension`、`option_cards`、`freeform_affordance.label` 和 `available_mode_switches`；5) 为 `test_messages_stream.py` 增加 SSE 与 session snapshot 对齐断言，直接比较结构化字段值而不是只比 suggestions 数量。</action>
  <acceptance_criteria>
    - `rg -n "guidance_mode|guidance_step|focus_dimension|freeform_affordance|available_mode_switches" apps/api/app/services/sessions.py apps/api/app/schemas/state.py` 返回命中
    - `rg -n "transition_reason|strategy_label" apps/api/app/services/sessions.py apps/api/tests/test_sessions.py` 返回命中
    - `cd apps/api && pytest tests/test_sessions.py tests/test_messages_stream.py -q` 通过
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && pytest tests/test_sessions.py tests/test_messages_stream.py -q</automated>
  </verify>
  <done>刷新恢复与流式消费使用同一份 guidance contract，session snapshot/turn decision meta/SSE payload 三处字段一致并通过回归。</done>
</task>

</tasks>

<verification>
后端验证必须同时覆盖：
- `decision.ready` 的结构化 guidance 字段存在且类型稳定
- session snapshot 刷新恢复不依赖前端猜测旧字段
- 同一轮 decision 的 SSE 与 session snapshot guidance 数据一致
</verification>

<success_criteria>
Phase 1 的后端 contract 完成后，前端只需消费 `guidance_mode`、`guidance_step`、`focus_dimension`、`transition_reason`、`option_cards`、`freeform_affordance`、`available_mode_switches` 即可表达本轮引导节奏，无需再从旧字段隐式推断。
</success_criteria>

<output>
After completion, create `.planning/phases/01-yin-dao-jie-zou-yu-xuan-xiang-cheng-qing/01-01-SUMMARY.md`
</output>
