---
phase: 01-yin-dao-jie-zou-yu-xuan-xiang-cheng-qing
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - apps/web/src/lib/types.ts
  - apps/web/src/store/workspace-store.ts
  - apps/web/src/components/workspace/action-options.tsx
  - apps/web/src/components/workspace/assistant-turn-card.tsx
  - apps/web/src/components/workspace/workspace-session-shell.tsx
  - apps/web/src/test/assistant-turn-card.test.tsx
  - apps/web/src/test/workspace-store.test.ts
  - apps/web/src/test/workspace-session-shell.test.tsx
autonomous: true
requirements:
  - GUID-01
  - GUID-02
  - GUID-03
  - GUID-04
must_haves:
  truths:
    - 用户在工作台里能明确看到当前是继续探索、比较选项还是开始确认
    - 高不确定节点会优先显示 2-4 个可反应选项，并始终保留“都不对，我补充”入口
    - 刷新会话后，上一轮 guidance 节奏、推进维度和切换原因仍能恢复
  artifacts:
    - path: apps/web/src/lib/types.ts
      provides: 前端 DecisionGuidance / DecisionReadyData 扩展字段定义
      contains: interface DecisionGuidance
    - path: apps/web/src/store/workspace-store.ts
      provides: SSE 与 snapshot guidance 派生真源
      contains: deriveDecisionGuidance
    - path: apps/web/src/components/workspace/assistant-turn-card.tsx
      provides: guidance 模式、原因、选项与自由补充入口渲染
      contains: AssistantTurnCard
  key_links:
    - from: apps/web/src/lib/types.ts
      to: apps/web/src/store/workspace-store.ts
      via: DecisionReadyData / DecisionGuidance
      pattern: guidanceMode|guidanceStep|focusDimension
    - from: apps/web/src/store/workspace-store.ts
      to: apps/web/src/components/workspace/assistant-turn-card.tsx
      via: decisionGuidance props
      pattern: optionCards|freeformAffordance|availableModeSwitches
    - from: apps/web/src/components/workspace/workspace-session-shell.tsx
      to: apps/web/src/test/workspace-session-shell.test.tsx
      via: hydrate session snapshot guidance
      pattern: transitionReason|strategyLabel
---

<objective>
消费 Phase 1 后端 guidance contract，让工作台在 UI 上稳定表达“先探索、再逐步收紧”的节奏、推进维度、选项式响应与自由补充入口。

Purpose: 满足用户可见层的引导体验要求，让前端不再从旧字段猜模式，而是直接渲染后端决定好的下一步动作。
Output: 扩展后的前端类型、workspace store guidance 派生、工作台 guidance UI 与对应 Vitest 回归。
</objective>

<execution_context>
@/Users/zhangyanhua/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhangyanhua/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-yin-dao-jie-zou-yu-xuan-xiang-cheng-qing/01-RESEARCH.md
@.planning/phases/01-yin-dao-jie-zou-yu-xuan-xiang-cheng-qing/01-01-SUMMARY.md
@apps/web/src/lib/types.ts
@apps/web/src/store/workspace-store.ts
@apps/web/src/components/workspace/assistant-turn-card.tsx
@apps/web/src/components/workspace/action-options.tsx

<interfaces>
From apps/web/src/lib/types.ts:
```typescript
export interface DecisionGuidance {
  conversationStrategy: DecisionStrategy;
  strategyLabel: string;
  strategyReason: string | null;
  nextBestQuestions: string[];
  confirmQuickReplies?: string[];
  suggestionOptions?: SuggestionOption[];
}

export interface DecisionReadyData {
  session_id: string;
  user_message_id: string;
  phase: string;
  conversation_strategy: string;
  next_move: string;
  suggestions: SuggestionOption[];
  recommendation: { label: string; content: string } | null;
  next_best_questions: string[];
}
```

From apps/web/src/store/workspace-store.ts:
```typescript
function deriveDecisionGuidance(decision: AgentTurnDecision): DecisionGuidance | null
function deriveDecisionGuidanceFromEvent(data: DecisionReadyData): DecisionGuidance
```

From apps/web/src/components/workspace/assistant-turn-card.tsx:
```typescript
interface AssistantTurnCardProps {
  decisionGuidance?: DecisionGuidance | null;
  onSelectDecisionGuidanceQuestion?: (question: string) => void;
  onRequestFreeSupplement?: () => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展前端 guidance 类型并统一 store 派生逻辑</name>
  <read_first>
    .planning/phases/01-yin-dao-jie-zou-yu-xuan-xiang-cheng-qing/01-RESEARCH.md
    .planning/phases/01-yin-dao-jie-zou-yu-xuan-xiang-cheng-qing/01-01-SUMMARY.md
    apps/web/src/lib/types.ts
    apps/web/src/store/workspace-store.ts
    apps/web/src/test/workspace-store.test.ts
    apps/web/src/test/workspace-session-shell.test.tsx
  </read_first>
  <files>apps/web/src/lib/types.ts, apps/web/src/store/workspace-store.ts, apps/web/src/components/workspace/workspace-session-shell.tsx, apps/web/src/test/workspace-store.test.ts, apps/web/src/test/workspace-session-shell.test.tsx</files>
  <behavior>
    - Test 1: `decision.ready` 事件能直接生成含 `guidanceMode`、`guidanceStep`、`focusDimension`、`transitionReason`、`optionCards`、`freeformAffordance`、`availableModeSwitches` 的 `decisionGuidance`
    - Test 2: 从 session snapshot hydrate 的 turn decision guidance 与 SSE 实时 guidance 使用相同派生逻辑
    - Test 3: 当只有 option cards 或只有 freeform affordance 时，store 仍保留可渲染 guidance，不会意外清空
  </behavior>
  <action>基于 Plan 01 的 contract 扩展前端数据模型和派生逻辑。具体要求：1) 在 `types.ts` 中为 `DecisionGuidance`、`DecisionReadyData`、`AgentTurnDecisionSectionMeta`、`AgentTurnDecisionStatePatch` 添加 `guidance_mode`/`guidanceMode`、`guidance_step`/`guidanceStep`、`focus_dimension`/`focusDimension`、`transition_reason`/`transitionReason`、`option_cards`/`optionCards`、`freeform_affordance`/`freeformAffordance`、`available_mode_switches`/`availableModeSwitches` 字段；2) 在 `workspace-store.ts` 中把 snapshot 与 event 两条路径统一到同一套 normalize/derive helper，保持旧 `conversationStrategy`、`strategyLabel` 兼容但不再作为唯一判断依据；3) 当 `freeform_affordance` 存在时，不要求必须有 `nextBestQuestions` 才显示 guidance；4) `workspace-session-shell.tsx` 只透传 store 中的 guidance，不额外拼装 mode 文案，避免前端二次猜测；5) 为 `workspace-store.test.ts` 和 `workspace-session-shell.test.tsx` 增加 refresh/hydrate 与 `decision.ready` 双路径断言，明确 compare/confirm 切换与 `transitionReason` 可见 per GUID-04。</action>
  <acceptance_criteria>
    - `rg -n "guidanceMode|guidanceStep|focusDimension|transitionReason|optionCards|freeformAffordance|availableModeSwitches" apps/web/src/lib/types.ts apps/web/src/store/workspace-store.ts` 返回命中
    - `rg -n "transitionReason|optionCards|freeformAffordance" apps/web/src/test/workspace-store.test.ts apps/web/src/test/workspace-session-shell.test.tsx` 返回命中
    - `cd apps/web && pnpm test -- src/test/workspace-store.test.ts src/test/workspace-session-shell.test.tsx` 通过
  </acceptance_criteria>
  <verify>
    <automated>cd apps/web && pnpm test -- src/test/workspace-store.test.ts src/test/workspace-session-shell.test.tsx</automated>
  </verify>
  <done>store 成为前端 guidance 的唯一真源，刷新恢复与流式更新都能稳定还原当前节奏、推进维度和切换原因。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 更新 guidance UI，让选项优先与自由补充入口同时可见</name>
  <read_first>
    .planning/phases/01-yin-dao-jie-zou-yu-xuan-xiang-cheng-qing/01-RESEARCH.md
    .planning/phases/01-yin-dao-jie-zou-yu-xuan-xiang-cheng-qing/01-01-SUMMARY.md
    apps/web/src/components/workspace/action-options.tsx
    apps/web/src/components/workspace/assistant-turn-card.tsx
    apps/web/src/test/assistant-turn-card.test.tsx
    apps/web/src/test/workspace-session-shell.test.tsx
  </read_first>
  <files>apps/web/src/components/workspace/action-options.tsx, apps/web/src/components/workspace/assistant-turn-card.tsx, apps/web/src/components/workspace/workspace-session-shell.tsx, apps/web/src/test/assistant-turn-card.test.tsx, apps/web/src/test/workspace-session-shell.test.tsx</files>
  <behavior>
    - Test 1: 卡片上可见 guidance 模式标签、推进维度或策略理由，用户知道当前是在探索、比较还是确认
    - Test 2: 高不确定节点渲染 2-4 个 option cards，并始终展示“都不对，我补充”入口 per GUID-03
    - Test 3: 若后端提供 `availableModeSwitches`，UI 能显示模式切换提示而非静默丢弃
  </behavior>
  <action>在不改现有消息流结构的前提下升级 guidance UI。具体要求：1) 在 `assistant-turn-card.tsx` 中显示 `guidanceMode` 和 `guidanceStep` 对应的中文节奏提示，保留 `strategyLabel` 作为主标题，但把 `transitionReason`、`focusDimension` 文案明确展示为“这轮为什么问这个”；2) `ActionOptions` 接收 `optionCards` 新字段时优先渲染其标题/说明，并继续支持旧 suggestion 兼容路径；3) 无论是否存在 option cards，只要 `freeformAffordance` 存在，就在 guidance 区内渲染一个明确可点击的“都不对，我补充”入口，不允许因 `suggestionOptions.length === 0` 被隐藏；4) 若 `availableModeSwitches` 非空，在 guidance 区显示“你也可以切到 xxx”一类提示 chips，但不要实现全局 mode switcher，遵守研究里的 defer；5) 更新 `assistant-turn-card.test.tsx` 与 `workspace-session-shell.test.tsx`，断言 mode 文案、freeform 按钮、option 数量上限和 hydrate 后 UI 可见状态。</action>
  <acceptance_criteria>
    - `rg -n "都不对，我补充|availableModeSwitches|guidanceMode|focusDimension|transitionReason" apps/web/src/components/workspace/action-options.tsx apps/web/src/components/workspace/assistant-turn-card.tsx` 返回命中
    - `rg -n "都不对，我补充|availableModeSwitches|transitionReason" apps/web/src/test/assistant-turn-card.test.tsx apps/web/src/test/workspace-session-shell.test.tsx` 返回命中
    - `cd apps/web && pnpm test -- src/test/assistant-turn-card.test.tsx src/test/workspace-session-shell.test.tsx` 通过
  </acceptance_criteria>
  <verify>
    <automated>cd apps/web && pnpm test -- src/test/assistant-turn-card.test.tsx src/test/workspace-session-shell.test.tsx</automated>
  </verify>
  <done>用户在工作台里能直接看到本轮引导节奏、选项式响应和自由补充入口，模式切换提示不会因前端缺字段而丢失。</done>
</task>

</tasks>

<verification>
前端验证必须同时覆盖：
- `decision.ready` 与 session snapshot hydrate 的 guidance 派生一致
- guidance UI 在 option cards 存在或不存在时都保留自由补充入口
- compare / confirm / explore 等节奏切换能被用户看见，不只是内部状态变化
</verification>

<success_criteria>
Phase 1 的用户可见层完成后，工作台能稳定呈现“继续深挖 / 比较选项 / 开始确认”的下一步引导，且高不确定节点优先给 2-4 个可反应选项并始终保留自由补充入口。
</success_criteria>

<output>
After completion, create `.planning/phases/01-yin-dao-jie-zou-yu-xuan-xiang-cheng-qing/01-02-SUMMARY.md`
</output>
