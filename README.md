# AI Co-founder V1

当前仓库仍处于初始化阶段：已经搭建好 monorepo 结构，并预置了根级的依赖管理、工作流脚本入口与相关配置，但各个子应用的实际实现尚在后续任务中逐步完成。

- `apps/api` 的应用壳体将由 Task 2 创建，当前目录仅保留空的 pyproject 定义。
- `apps/web` 的页面与设计工作台会在 Task 8/9 中先后建立，目前目录仅包含依赖列表和脚本的占位 package。
- 根目录提供了 `dev:web`、`dev:api`、`test:web`、`test:api` 四个脚本来统一启动或测试各子项目，但在上述任务完成前，不承诺这些命令都可以被执行成功。

请务必基于前述任务的进展再展开实际工作，否则相关 `dev`/`test` 脚本可能因缺少实现而运行失败。
